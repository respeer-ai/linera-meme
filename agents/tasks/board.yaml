version: 1
type: task-board
authority: high
status_legend:
  Todo: not started
  Doing: in progress
  Done: completed
  Blocked: waiting on dependency or confirmation
groups:
  - name: Positions
    tasks:
      - id: POS-001
        status: Done
        priority: P2
        depends_on: []
        title: Positions tab / route
        notes: 页面入口已存在
      - id: POS-002
        status: Done
        priority: P2
        depends_on: [POS-001]
        title: Positions 原型页
        notes: 当前为静态原型页
      - id: POS-003
        status: Done
        priority: P2
        depends_on: [POS-002]
        title: V2 文案语义校正
        notes: |
          已去掉 V3 风格 `in range / rewards / incentives`
      - id: POS-004
        status: Done
        priority: P2
        depends_on: [POS-002]
        title: Status UI 改成 All / Active / Closed
        notes: 当前仅 UI 尚未接真实数据
      - id: POS-005
        status: Todo
        priority: P2
        depends_on: []
        title: 明确 opened_at 定义
        notes: 取首次 add 还是最近一轮 reopen 时间
      - id: POS-006
        status: Todo
        priority: P2
        depends_on: [POS-005]
        title: 明确 current_liquidity 与 LMM 展示关系
        notes: 是否直接用作顶部 share 数值
      - id: POS-007
        status: Done
        priority: P1
        depends_on: []
        title: service/kline 实现 get_positions(owner status)
        notes: 已完成 owner 维度 positions 聚合查询
      - id: POS-008
        status: Done
        priority: P1
        depends_on: [POS-007]
        title: service/kline 暴露 GET /positions
        notes: 已完成统一查询接口
      - id: POS-009
        status: Done
        priority: P1
        depends_on: [POS-007]
        title: 为 positions 聚合补 DB 单测
        notes: 已覆盖 active closed 多池 多用户 非法状态
      - id: POS-010
        status: Done
        priority: P1
        depends_on: [POS-008]
        title: 为 /positions 补接口测试
        notes: 已覆盖 owner status 非法参数
      - id: POS-011
        status: Done
        priority: P1
        depends_on: [POS-008]
        title: 前端新增 positions store/types/wrapper
        notes: 已新增独立 positions 数据层
      - id: POS-012
        status: Done
        priority: P1
        depends_on: [POS-011]
        title: Positions 页接钱包 owner
        notes: 已按当前钱包 owner 拉取 positions
      - id: POS-013
        status: Done
        priority: P1
        depends_on: [POS-008, POS-011, POS-012]
        title: Positions 页接 /positions
        notes: 已拉取真实 active / closed 数据
      - id: POS-014
        status: Done
        priority: P1
        depends_on: [POS-013]
        title: 实现 Status 筛选联动接口
        notes: 已支持 all active closed 查询切换
      - id: POS-015
        status: Done
        priority: P1
        depends_on: [POS-013]
        title: 实现真实列表渲染
        notes: 已用真实 position card 替换纯空态原型
      - id: POS-016
        status: Done
        priority: P2
        depends_on: [POS-015]
        title: 空态区分未连接钱包与无持仓
        notes: 已区分钱包未连接 无 active 无 closed
      - id: POS-017
        status: Done
        priority: P2
        depends_on: [POS-015]
        title: 设计 active position card
        notes: 已展示 pool token share opened activity 信息
      - id: POS-018
        status: Done
        priority: P1
        depends_on: [POS-015]
        title: 接 Manage / Remove liquidity 入口
        notes: 已支持从 active position 进入 remove/manage 页面
      - id: POS-019
        status: Blocked
        priority: P0
        depends_on: []
        title: 核验 from_account 是否稳定代表 LP owner
        notes: 依赖后端交易写库语义确认
      - id: POS-020
        status: Blocked
        priority: P0
        depends_on: []
        title: 核验 liquidity 字段是否可直接净额相减
        notes: 依赖后端字段单位和精度确认
      - id: POS-021
        status: Done
        priority: P0
        depends_on: [POS-013]
        title: 保持 /positions 为轻量列表接口
        notes: 已实现 ALLOWED_FIELDS frozenset 白名单，serializer 过滤非 Position 上下文字段
      - id: POS-022
        status: Done
        priority: P0
        depends_on: [POS-021]
        title: service/kline 新增 position metrics 接口
        notes: |
          已新增 `/position-metrics`，active query path 已切到 projection-backed snapshot/replay inputs。
          Identity PositionMetricsSerializer 已删除（内联到 handler）。
          8 个 legacy facade/support 文件已删除，compatibility 目录已清理。
      - id: POS-023
        status: Doing
        priority: P0
        depends_on: [POS-022]
        title: 前端接入真实 position fee 展示
        notes: |
          前端已切到消费 `/position-metrics`，不再使用 24h 成交量近似 fee。
          当前 `exact_fee_supported=false` 时仍显示 `Syncing`，这只是临时占位。
          在 POS-022/024 完成前，不要引入与当前算法无关的 TVL/24h volume 近似。
      - id: POS-025
        status: Doing
        priority: P0
        depends_on: [POS-022]
        title: position-metrics 改为与池历史长度无关的物化查询
        notes: |
          `latestTransactions` 相关依赖已从 active read path 移除。
          当前 `/position-metrics` 查询使用：
          - snapshot inputs projection
          - replay facts projection
          - projection-backed payload adapter

          当前新增收敛：
          - settled trade/liquidity -> product transaction row 已收敛到单一 canonical adapter
          - `position-metrics` 查询装配已集中到 `KlinePositionMetricsDependencies`；
            runtime/entry/debug 不再各自复制 fetcher/repository/provider 拼装
          - `PositionMetricsSnapshotSourceRepository` 已被更明确的
            `PositionMetricsSnapshotMaterializationInputsRepository` 取代；
            Layer 3 snapshot materialization 当前显式消费
            `SettledTradeProjectionRepository` / `SettledLiquidityProjectionRepository`
            与 fee-to history 输入，不再保留模糊 snapshot-source 概念
          - 已删除 `PositionMetricsTransactionHistorySupport` pass-through；gap summary 直接并回 assembly adapter
          - 已删除 `PositionMetricsPoolApplicationClientFactory` pass-through；live payload API 直接持有 client type 与 URL 解析支持
          - 已删除 `PositionMetricsAssemblyHelperCluster`、`PositionMetricsSupportFactory`、`PositionMetricsModuleAssembly`
            三层装配壳，position-metrics 当前收敛为更直接的 bootstrap/object graph
          - 已继续删除 `PositionMetricsAssemblyAdapterCluster` 壳层；support graph 现直接由
            `PositionMetricsBootstrap` 持有，不再存在 assembly 包 assembly 的转发层
          - 已删除 `PositionMetricsReplayInputsProjectionRepository`；active path 当前直接依赖
            `PositionMetricsReplayFactsProjectionRepository` 作为 replay/history canonical 边界
          - 已删除测试专用薄入口 `position_metrics.py`；调用方现直接依赖
            `PositionMetricsBootstrap`，避免继续保留无概念的 public-api shell
          - 已删除 `KlineTickerRuntimeState` 状态搬运盒；ticker 生命周期状态现内聚在
            `KlineEntrypointServices`，不再在 `kline.py -> services -> lifecycle`
            之间额外搬运三元状态
          - 已把 `KlineDebugService` 拆成 `KlinePositionMetricsDebugService` 与
            `KlineObservabilityDebugService` 两个明确子服务；原对象现仅保留编排职责
          - 已删除 `PositionMetricsLiquidityHistorySupport`、`PositionMetricsSwapSupport`、
            `PositionMetricsReplaySupport` 三个 forwarding shell；`PositionMetricsBootstrap`
            与 `PositionMetricsReplayEntrypoint` 当前直接持有真实协作者，不再通过
            “support 总线对象”转发工厂与方法
          - 已删除 `PositionMetricsPoolApplicationSupport` 壳层；public-account 解析已收回
            `AccountCodec`，pool application URL/query 构造已收回 `PoolApplicationClient`，
            live payload path 不再通过一个混合 support 对象转发三种不同语义
          - 已删除 `KlinePositionMetricsEntrypointOverrides` 类壳；入口级测试注入当前直接使用
            简单 override state 字典，`KlinePositionMetricsDependencies.resolve` 直接消费
            builder/override 字段，不再保留仅为测试 seam 命名的对象
          - `KlinePositionMetricsDebugService` 当前不再穿透
            `replay_facts_repository.settled_pool_history_projection_repo` 读取池历史；
            `KlinePositionMetricsDependencies` 已显式暴露 `pool_history_repository()`，
            让 debug/tooling 与 active runtime 共用同一 projection contract，而不是依赖
            replay repo 的内部实现字段
          - query-stack / position-metrics 测试支撑已开始去除旧 payload 形状兼容；
            `PositionMetricsProductStateQueryInputProvider`、`fetch_context`、
            `fetch_coordinator` 与 `QueryStackReadModelTestSupport` 当前统一使用
            `PositionMetricsSnapshotInputs` 和 `PositionMetricsReplayBundle/Facts`
            canonical object，不再把 dict 形状当作主 contract
          - `PositionMetricsSnapshotProjectionRepository` /
            `PositionMetricsSnapshotInputsProjectionRepository` 当前也已直接返回
            `PositionMetricsSnapshotInputs` canonical object；`snapshot/replay` 边界从
            projection repo 到 query stack 已统一，不再依赖 query 层二次包装 dict 载荷
          - `PositionMetricsSnapshotBuilder` /
            `PositionMetricsSnapshotMaterializationInputsRepository` 当前已把 pool rebuild 输入
            收敛到单一 `list_pool_transaction_history()` canonical boundary，不再在 snapshot
            rebuild 路径上分别抓 trade/liquidity 两路列表再本地拼接
          - ticker 运行时不再借 `PositionMetricsReplayInputsProjectionRepository`
            间接拿池历史；当前直接依赖 `SettledPoolHistoryProjectionRepository`
            作为 Layer 3 市场历史边界，避免 position-metrics 子图泄漏到行情主路径
          - 旧 `transactions/candles` 表上的历史 candle 查询与 rebuild 语义已抽到
            `LegacyCandleMaterializationTool`；`Db` 当前仅保留向后兼容转发与低层 candle 写入/materialization 能力，
            不再把这层历史工具语义和 active runtime projection path 混在一起

          剩余工作：
          1. 清理不再属于 active runtime 的 legacy facade/query helper
          2. 缩减只为旧 payload 形状存在的兼容测试夹具
          3. 继续把独立工具入口与主运行时语义对齐到同一 projection contract
          4. 后续把 snapshot/replay inputs 的上游从当前 settled/product projections 继续推进到完整 block-parse-backed product projections
      - id: POS-043
        status: Done
        priority: P0
        depends_on: [POS-034, POS-035]
        title: 删除 position-metrics 对 latestTransactions 的所有依赖
        notes: |
          Python 测已全部清除 latestTransactions 引用。
          active runtime `/position-metrics` 已确认只走 projection-backed snapshot/replay inputs，不再引用 `latestTransactions`。
      - id: POS-026
        status: Done
        priority: P0
        depends_on: []
        title: 设计基于链区块的可靠观测与事件同步系统
        notes: |
          架构设计已完成。三层架构（Layer 1 raw → Layer 2 normalized → Layer 3 market-data）已落地。
          当前所有 observability tests 通过。499/548 测试通过，49 个 ERROR 均为环境依赖（MySQL/network）。
      - id: POS-031
        status: Done
        priority: P0
        depends_on: [POS-026]
        title: 设计 Layer 1 raw schema 与主键
        notes: |
          DDL 已落地 raw_repo.py。Operational tables（chain_cursors, processing_cursors, ingestion_anomalies, raw_block_ingest_runs）已定义。Raw root/child tables 已定义。
          RawRepository 14 tests pass。下一步连接 bootstrap 见 POS-032。
      - id: POS-032
        status: Doing
        priority: P0
        depends_on: [POS-031]
        title: 实现 Layer 1 block ingestion 与 cursor
        notes: |
          按 `chain_id + height` 顺序抓取、落库和推进 `chain_cursors`，支持补拉、重放、重启恢复。
          当前硬目标：它必须成为唯一原始事实入口，后续 Layer 2/3 与产品读路径不能再回头读 `latestTransactions`。
          当前已完成：
          - `IngestionCoordinator` 的 `mark_attempt -> fetch_block -> parse -> ingest_block -> mark_failure` 骨架
          - `LayerOneBlockParser`、`LineraGraphqlChainClient`、`ChainCursorStore`
          - event-driven `CatchUpRunner` / `CatchUpDriver` / `ChainEventProcessor` / `LineraGraphqlNotificationListener`
          - `GET /debug/observability`
          - fail-open lifecycle 已剥离到 POS-042
          - decoder/normalizer 层已接入 POS-033/034
      - id: POS-042
        status: Done
        priority: P0
        depends_on: [POS-032, POS-036]
        title: 重构 observability 为单进程内 fail-open 子生命周期
        notes: |
          已完成。ObservabilitySupervisor 提供 start_in_background() / recover() / shutdown() 与 fail-open 语义。
          KlineServiceLifecycle.startup() 对 observability 异常做 fail-open 吞吐。
          fail-open lifecycle test 已覆盖 "后台启动同步抛错时 kline startup 仍成功" 场景。
      - id: POS-033
        status: Doing
        priority: P0
        depends_on: [POS-042]
        title: 设计 application registry 与 decoder registry
        notes: |
          为 `pool`、`swap`、`meme`、`proxy`、`ams`、`blob-gateway` 建立 app 类型注册与 decoder 选择规则。
          当前已完成：
          - 持久化 `application_registry` 与 MySQL repo
          - `ApplicationRegistry`、`DecoderRegistry`、`DecoderDispatcher`
          - `SwapCatalogClient`、`ProxyCatalogClient`、`ApplicationDiscoveryService`
          - Rust canonical decoder 路径接线
          - `DecodeScheduler`
          - `proxy/ams/blob-gateway` message decoder 接线补齐，已不再把这些已声明 pair 落为 `unimplemented_decoder`
          - **Rust decoder crate 从 `abi/` 迁移到 `service/decoder/`**（职责分离，不入合约 ABI crate）
          - **子进程 batch 优化**：Rust 端接收 JSON 数组、返回每项独立 `status: ok/error`；
            Python `RustDecoderRunner.decode_batch()` 一次子进程处理整 block；
            `DecodeScheduler.decode_batch()` 已接入管线，不再逐条启动子进程
          当前硬约束：
          - 复杂 ABI/BCS payload 不再用 Python 手写 field-level BCS
          - decode 失败不阻断 ingestion
          下一步是把 canonical decode 结果继续接入 Layer 2 business families 与 Layer 3 market semantics。
          Decoder registry 已完备。6 app types (pool/swap/meme/proxy/ams/blob-gateway) 均已注册对应 decoder。
      - id: POS-034
        status: Doing
        priority: P0
        depends_on: [POS-033]
        title: 定义 Layer 2 normalized events 与关联键
        notes: |
          统一定义业务事件字典、source/target 关联键、decode failure 记录，以及 `Reject` 到业务事件的映射。
          当前已完成：
          - `normalized_event_result`、`decode_result_normalizer`
          - `normalized_events` 表与 materializer
          - `processing_cursor_repo`、`normalization_worker`、`normalization_replay_driver`
          - `POST /debug/normalization/replay/run`
          - 第一批 pool-specific Layer 2 families
          - proxy operation/message families 不再落 generic application fallback；`meme_created` 等已具备显式 Layer 2 family
          - ams/blob-gateway operation/message families 改为显式 Layer 2 families，不再落 generic application fallback
          - meme `creator_chain_id` operation 改为显式 Layer 2 family
          - `post_ingest_pipeline` 挂到 `CatchUpRunner`
          当前仍缺：
          - 继续审视是否还有 decode 成功但仍落 generic fallback 的业务 payload
          - 后续按 Layer 3 真实消费需求再补更细 reject/business family
          - pool payload field-level decode 的完整性继续随 canonical decoder 演进校验
          - `raw_events` 已接入 Layer 1 schema、candidate listing 与 normalization replay 默认输入
          - 当前阶段以 block-observed `PoolMessage::NewTransaction` 作为新交易观测事实；
            未来若官方提供类 EVM event 能力，可再替换 carrier
          - `new_transaction` 的 Layer 2 family 已从 legacy wording 收敛为
            `pool_new_transaction_recorded`
          - 已补 matrix-style normalizer coverage，显式锁住 pool/swap/meme/proxy/ams/blob-gateway
            已声明 family 分支，避免 decode 成功后悄悄退回 generic fallback
      - id: POS-035
        status: Doing
        priority: P0
        depends_on: [POS-034]
        title: 定义 Layer 3 market-data 契约并替换 latestTransactions
        notes: |
          已落第一版可执行 Layer 3 主干，并把 `market_deriver` 从 planned 提升为真实 worker boundary。
          当前已完成：
          - `settled_market_result`、`settled_market_deriver`、`settled_market_materializer`
          - `market_derivation_worker`、`market_derivation_replay_driver`
          - `settled_trades`、`settled_liquidity_changes` MySQL repo 与 schema
          - `POST /debug/market-derivation/replay/run`
          - Layer 3 replay/materialization 基础骨架
          - Layer 3 runtime 已切到 `raw_posted_messages + pool_new_transaction_recorded`
          - `settled_trade / settled_liquidity_change` 当前直接从
            `decoded_payload_json.transaction` 派生
          - Layer 3 下游 product transaction history 语义已开始统一；
            settled trade/liquidity/snapshot builder 共用同一 settled->product transaction adapter
          - 已收紧 settled->product transaction adapter contract：
            trade history row 在 projection column 缺失时，会统一回落到
            canonical `event_payload_json.transaction` 字段，避免 trade/liquidity
            下游读取同一 Layer 3 事实时出现空字段分叉
          - 已把 `decoded_payload_json.transaction.transaction_type` 的 canonicalization
            上推到 decoder 边界：`pool_message` / `swap_operation` / `swap_message`
            当前统一输出 `AddLiquidity/RemoveLiquidity/BuyToken0/SellToken0`，
            不再把 legacy snake_case 留给 Layer 3 与 projection 下游各自兼容
          - 已把 `NewTransaction.transaction` 的 canonical 时间/owner 读取合同收敛到
            `PoolNewTransactionExecutionFact` 边界：当前统一读取
            `created_at_micros -> ms`、`created_at -> canonical ms`、
            `from` 对象、显式 `from_account`、以及 settled owner 字符串 `owner@chain`
            的 position-owner 回退；避免 Layer 3/product read model 再各自追加 rescue 逻辑
          - 已补 runtime assembly guard，锁定 ticker transaction history 注入为
            projection-backed `SettledPoolHistoryProjectionRepository`
          - 已补 maker stall diagnostics guard，锁定 latest-db-transaction watermark
            来自 `TransactionWatermarksQueryRepository -> settled_trades`
          - Layer 3 snapshot materialization 输入边界已显式命名为
            `PositionMetricsSnapshotMaterializationInputsRepository`，
            不再沿用旧的 snapshot-source 术语
          当前硬要求：
          - `latestTransactions` 必须从 `transactions/candles/positions/fees/pool/position-metrics/debug audits` 路径全部删除
          - Layer 3 必须成为唯一产品输入
          - 第一阶段允许使用区块中的 `PoolMessage::NewTransaction` 作为新交易观测事实，
            但不得使用 pool `latestTransactions` 状态窗口
          - `settled_trade / settled_liquidity_change` 必须直接从区块中的真实 pool 执行事实派生
          当前仍缺：
          - `NewTransaction` 作为 observability fact 只是暂时方案；
            未来若官方提供类 EVM event 能力，可再切换 carrier
          - 继续把其余 Layer 3 和产品投影统一到当前 `NewTransaction.transaction` contract
          - 当前仍未把所有产品读路径全部切到 Layer 3，也还未处理更复杂的跨事件关联与完整产品投影。
          Python side latestTransactions cleanup complete. Rust contract side remains in pool state/service for future contract deployment.
      - id: POS-044
        status: Doing
        priority: P0
        depends_on: [POS-035]
        title: 删除交易/K线/debug audit 对 latestTransactions 的所有依赖
        notes: |
          明确为删除型 cutover。
          范围：
          1. 删除 `swap.py`、`kline.py` 等仍直接查询 `latestTransactions` 的实现
          2. recent audit 改为基于 Layer 1/2/3 持久化状态与 processing cursor 做诊断
          3. `/points`、`/transactions`、`/positions`、`/position-metrics`、相关 debug surface 全部改为只读 projection/read-model
          4. 合约侧移除 `latestTransactions` 后，服务端不需要任何兼容分支
          当前已完成：
          - 删除 `/transactions/audit/recent` 与 `/debug/pool` 中的 live recent audit 参数/返回
          - `/debug/pool` 改为只读 replay/snapshot projections，不再直连 legacy DB history/gap/liquidity helpers
          - `/diagnostics`、`/debug/traces` 与 `/debug/pool` 的 diagnostics 子查询改为显式 query repositories，不再由 `kline.py` 直接调用 `_db` 查询
          - `/transactions/audit/replay` 改为只读 projection-backed pool transaction history
          - `ticker.py` 改为 projection-only consumer
          - `Ticker` 显式注入 projection transaction-history capability，不再经由 `Db.get_pool_transaction_history()` 取池交易
          - projection-backed pool/position transaction history 的 trade/liquidity row 语义已统一，减少 Layer 3 下游 read model 漂移
          - `pool_new_transaction_recorded -> execution fact -> settled outputs` 的时间与 owner/account
            读取合同已收敛，相关 product/query 路径不再需要各自兼容 `created_at_micros`
            与 owner 字段分叉
          - `swap.py` 的 pools GraphQL 查询已移除死字段 `latestTransaction`，外围运行路径不再读取该旧合约窗口字段
          - `Db` 上遗留的 `get_pool_transaction_history / get_pool_swap_count_since / get_pool_transaction_gap_summary`
            已删除，active runtime 不再保留旧 transactions-table pool-history 入口
          - `Ticker` 启动 watermark 读取改为显式 `TransactionWatermarksQueryRepository`，不再经由 `Db.get_latest_transaction_watermarks()`
          - `maker_api.py` 的 maker events / traces / pool stall 读路径改为显式 query repositories，不再由 API handler 直接调用 `_db` 上的对应查询方法
          - 已补 runtime guard：`kline_runtime` 的 `/points` `/transactions` `/positions` 与 ticker history
            装配必须走 settled projection repositories；`maker_api_runtime` 的 stalled-pool 诊断必须走
            `PoolCatalogQueryRepository / MakerEventsQueryRepository / DebugTracesQueryRepository /
            TransactionWatermarksQueryRepository`
          - 当前剩余 `Db` 依赖主要是入口壳构造、连接生命周期和遗留工具脚本；
            active truth path 已不再调用 `db.py` 上的 legacy transaction/position query 方法
          - `/ticker`、`/poolstats`、`/protocol/stats` 改为读取 Layer 3 `settled_trades` 驱动的 `market_stats_projection_repo`
          - `swap.py` 删除 `get_pool_transactions()`
          - 删除空转的 `compatibility/legacy_response_shapes.py` facade
          - 删除 `totalSupply` unknown-field legacy query fallback
          - websocket 初始 K 线快照切到 projection candle reader
          - 删除失活的 `Db.get_last_kline()`
          - active runtime handler wiring 不再经过 `ProjectionRepository`
          - `ticker` 与 `maker_api` 的最新交易水位读取已改为 `settled_trades` projection，
          - 已完成一轮 active-path 审计：`/points`、`/transactions`、`/positions`、
            `/position-metrics`、`/debug/pool`、`/diagnostics`、`/debug/traces`、
            maker stall diagnostics 当前均走 projection/query repositories；
            仍留在仓库内的 `Db` 旧查询主要属于底层连接壳、sidecar CLI 或未切除的遗留实现，
            不是当前产品 truth path
            不再读取旧 `transactions` 表
          - `Ticker` 已改为只依赖 `PoolCatalogWriter` 与 projection readers，
            不再直接持有 `Db` 大对象
          - `kline.py` 已完成第一轮入口瘦身：
            runtime 装配下沉到 `kline_runtime.py`，
            ticker/observability 生命周期下沉到 `kline_service_lifecycle.py`，
            debug/readiness 聚合下沉到 `kline_debug_service.py`
          - `Swap` 已改为接收显式 resolved query base URL，内层对象不再依赖
            `running_in_k8s()` 拼接环境特定路径
          - `Proxy`、`Meme`、`SwapCatalogClient`、`ProxyCatalogClient` 已统一改为
            接收显式 resolved base URL；`service/kline/src` 活跃运行时已无
            `running_in_k8s()` 调用残留
          - `/position-metrics` active query path 改为 projection-backed snapshot/replay inputs
          - `Db.get_kline()` 删除 query-time transaction fallback，只读 materialized candles
          - `Db.load_candle()` 不再读时从 transactions 偷修 legacy candle
          - candle 写路径仅在命中 legacy bucket 时做受控单桶 repair，不回退产品 query 语义
          - 已确认另一类结构性脏点：`running_in_k8s` / `in_k8s` 被透传进 `position_metrics*` 旧 facade/public API/runtime 与若干旧 client；约束已记录为“内层只收 resolved endpoint/full base URL，不得感知部署环境布尔值”
          - `position-metrics` 旧 live payload 路径已切到 full `application_url`/resolved base URL contract，不再在该链路内传播 `in_k8s` / `running_in_k8s`
          - 删除 `PositionMetricsProvider` 死包装层
          - 删除 `position_metrics.py/public_api/entrypoint` 上空转的 `pool_application_url` facade 暴露
          - 删除 `PositionMetricsLiveHistoryReconciler` 空转层，planner/enricher 现在直接消费
            projection 提供的 liquidity history / pool transaction history / gap summary
          - `service/kline/src/compatibility/` 已不再承载活跃运行时兼容逻辑，仅保留空包占位说明

          额外架构约束：
          - “内层只收 resolved endpoint/full base URL” 只是中间态，不是目标态
          - 目标态应由外层装配 configured HTTP/transport client，内层依赖 client capability，而不是继续层层传 URL string
          - 不允许继续保留没有独立语义的 facade/pass-through object；此类对象应在当前主线中持续收敛

          当前 active product path 中，legacy candle fallback 只剩 `db.py` 旧模块内部及其遗留测试；
          active runtime `/position-metrics` 已确认消费 projection-backed snapshot/replay inputs；
          下一步重点转向清理剩余 legacy/compatibility 文档与继续把 Layer 3 product projections
          和 observability projections 语义收紧。
          Python kline source-side cleanup done. Contract-side latestTransactions removal deferred to contract deployment.
      - id: POS-036
        status: Doing
        priority: P0
        depends_on: []
        title: 创建 kline phase-1 新包骨架
        notes: |
          按 `agents/primitives/kline-package-layout.md` 创建 phase-1 所需的最小骨架。
          当前已落地：
          - `app/{config,bootstrap,lifecycle}`
          - `integration/chain_client.py`
          - `storage/mysql/{connection,raw_repo,projection_repo}.py`
          - `ingestion/{coordinator,cursors,anomalies}.py`
          - `projection/{projector,trades,candles,positions,pools}`
          - `query/{handlers,read_models,serializers}`
          - `compatibility/legacy_response_shapes.py`
          并已通过 `py_compile` 做最小导入校验。
      - id: POS-037
        status: Doing
        priority: P0
        depends_on: [POS-035, POS-036, POS-042]
        title: 实现 transactions 与 candles 的 phase-1 read models
        notes: |
          已落 `query.read_models.transactions` / `query.read_models.candles`、对应 handlers 与 serializers，并把 endpoint runtime 切到新查询栈。
          当前 `ProjectionRepository` 已只走 projection boundary：
          - `settled_trade_projection_repo` 直接从 Layer 3 `settled_trades` 生成 `/transactions`
          - `/points` 也从同一来源按 interval 聚合 candle points
          - 新增 `pool_identity_projection_repo.py`
          - 新增 `pool_transaction_history_projection_repo.py`
          当前 targeted unittest 已覆盖 settled-trade rows、candle 聚合、pool identity 和 shared history boundary。
      - id: POS-038
        status: Doing
        priority: P0
        depends_on: [POS-035, POS-036, POS-042]
        title: 实现 positions 的 phase-1 read model
        notes: |
          已落 `query.read_models.positions`、对应 handler 与 serializer，并把 `/positions` 切到新查询栈。
          当前 `ProjectionRepository.get_positions()` 与 `PositionMetricsProjectionRepository.get_positions()` 都已只走 `settled_liquidity_projection_repo`，不再回退 `Db.get_positions`。
          `position-metrics` 的 phase-2 迁移也继续前推：
          - 引入专用 `storage/mysql/position_metrics_projection_repo.py`
          - 新增 `query.read_models.live_position_metrics_fetcher`
          - 远端 pool GraphQL 请求细节进一步收口到 `integration/pool_application_client.py`
          执行约束：禁止补偿实现。
      - id: POS-039
        status: Doing
        priority: P0
        depends_on: [POS-037]
        title: 切换 points 与 transactions handlers 到新 read models
        notes: |
          已完成主 read-path cutover：
          - `/points/...`
          - `/points/.../information`
          - `/transactions/...`
          - `/transactions/information`
          以上路径现都通过 `query.handlers.* + query.serializers.* + projection repos` 读数据，不再在 endpoint 内调用 legacy `Db`，也不再保留 runtime fallback。
      - id: POS-040
        status: Done
        priority: P0
        depends_on: [POS-038]
        title: 切换 positions handler 到新 read model
        notes: |
          `/positions` 已切到 `query.handlers.positions` 和 `query.serializers.positions`，由新 read model 返回 owner envelope。
          当前 handler runtime 只走 projection 契约，`settled_liquidity_projection_repo` 负责 positions 与 owner/pool liquidity history。
          `position-metrics` 侧的 live fetch / replay / payload enrichment / repo contract 已完成多轮对象化与 bundled-contract 收口。
          当前稳定边界：
          - read path 消费 `get_replay_facts()` 与 `get_snapshot_inputs()`
          - runtime 通过公开 `build_runtime()` / `build_replay_entrypoint()` 获取 position-metrics 能力
          - `PositionMetricsPublicApi` 已成为稳定入口
          后续继续把 replay-time derivation 从 query/read boundary 下推到 projection/repository 契约。
      - id: POS-041
        status: Done
        priority: P0
        depends_on: [POS-039, POS-040]
        title: 为 phase-1 迁移增加 parity 与回退护栏
        notes: |
          parity / rollback runtime 方案已废弃。
          当前目标明确为纯新架构，不再允许 runtime legacy path、handler shadow compare 或 rollout debug surface。
          之前规划中的 `compatibility/legacy_response_shapes.py` 未成为活跃运行时依赖，
          现已不再保留兼容层运行时代码。
      - id: POS-045
        status: Todo
        priority: P1
        depends_on: [POS-044]
        title: review kline OO 架构并消除 facade/URL 透传中间态
        notes: |
          专门的结构审查任务，不在当前主线切题范围内扩散实现。
          目标：
          - review `service/kline` 当前新旧模块边界是否符合 OO 概念分层
          - 识别没有独立语义、只有委托行为的 facade/pass-through object
          - 把“resolved endpoint/full base URL” 中间态继续收敛为外层注入 configured client / transport object
          - 记录需要重做而不是值得补丁式优化的结构点
      - id: POS-027
        status: Doing
        priority: P0
        depends_on: []
        title: 排查生产环境 K 线自 03:00 起归零原因
        notes: |
          聚焦线上已发生故障：用户最初描述为“03:00 起归零”，已校正为“03:00 之后没有新蜡烛、价格一直横着”。
          当前结论：
          - production 活跃池 1000/1001/1002 的 `/points` 在 2026-04-20 03:00 CST 后并未返回 `close=0`，而是继续返回非零价格
          - recent live-vs-DB audit 无缺口，因此不是 recent window 丢交易导致的“假平线”
          - 早期一轮排查曾指向 maker-like 地址库存枯竭与 `maker-wallet-service` OOM
          - 新一轮线上取证又确认 maker 仍持续向 wallet 发起 `swap` mutation，wallet balances 与 token inventory 正常
          - 三个活跃池的 live `latestTransactions` 与 kline DB 都停在更早时间点，断点位于 wallet/chain 执行层，不在 kline 同步

          当前排障重点已经收敛到 POS-028/029/030：确认 maker-wallet OOM/重启、listener、证书处理与 creator 链 `Reject` 之间的真实因果链。
      - id: POS-028
        status: Done
        priority: P0
        depends_on: [POS-027]
        title: maker-wallet-service 暴露 8082 metrics 端口
        notes: |
          已在 `k8s/maker/02-deployment.yaml` 的 `maker-wallet-service` 容器声明 `containerPort: 8082`，
          并在同文件的 Service 增加 `maker-wallet-metrics` 端口，保持对外仍只走业务 `8080`。
          部署后可由 `maker-service` 通过
          `http://maker-wallet-service-$index.maker-wallet-service:8082/metrics`
          拉取指标。
      - id: POS-029
        status: Done
        priority: P0
        depends_on: [POS-028]
        title: maker-service 新增 wallet debug/export API
        notes: |
          已直接扩展现有 `service/kline/src/maker_api.py`，复用 `Db` 与现有 trace/event 表，
          在 maker-service 原 `8080` 上提供只读 debug API，避免再起第三个进程和新端口。
          当前已落地：
          - `/debug/wallets`
          - `/debug/wallets/{index}/metrics`
          - `/debug/wallets/{index}/balances`
          - `/debug/traces`
          - `/debug/pools/stall`
          - `/debug/health`

          接口会聚合 wallet metrics、wallet GraphQL 余额、maker DB 事件和 pool 最新交易水位，
          并返回语义化 health/stall 结论。
      - id: POS-030
        status: Doing
        priority: P0
        depends_on: [POS-029]
        title: maker wallet 执行断点诊断闭环
        notes: |
          已在 `/debug/pools/stall` 聚合最近 maker event、wallet swap trace、DB 最新 transaction 和
          wallet health，并输出 `lag_seconds` 与 `suspected_stage`。
          当前已覆盖：
          - `maker_idle`
          - `wallet_metrics_unreachable`
          - `wallet_low_gas`
          - `wallet_memory_high`
          - `mutation_accepted_but_not_settled`
          - `unknown`

          已确认的关键事实：
          - 2026-04-22 线上 maker-service CrashLoop 的直接原因不是 wallet，而是
            `service/kline/src/db.py` 启动时对超大 `maker_events` 表执行 `ensure_debug_indexes()`，
            MySQL 返回 `1114 The table 'maker_events' is full`
          - 现已把 debug 索引创建与 debug 表写入改为 best-effort，表满时只记录
            `debug_storage_degraded`，不再阻断 maker 启动和主交易循环
          - 后续线上复核已推翻“没有稳定 inbox 处理者”的中间结论：pool creator 链仍在持续出块并处理 inbox
          - 最近用户链发来的 `PoolMessage::Swap` 在 creator 链上稳定表现为 `ReceiveMessages + Reject`
          - maker trace 显示 wallet mutation 持续返回 200，但 pool `latestTransactions` 与 swap root
            仍停在旧时间窗，同时 pool 合约真实资产余额与 `reserve` 明显漂移

          结论：断点已收敛到“资金/消息已到 creator 链，但 swap 执行阶段发生硬失败”，
          而不是 kline 漏同步或 creator 链无人监听。剩余工作是把一条最近 reject 对齐到具体
          panic/运行时失败点，并补足链上与 DB 调试数据。
      - id: POS-024
        status: Doing
        priority: P0
        depends_on: []
        title: pool/swap 语义对齐 Uniswap V2
        notes: |
          已锁定为固定 Uniswap V2 fee-on 语义，并完成以下核心对齐：
          - 移除未生效的 `protocol_fee_percent_mul_100`
          - swap 输出改为 constant-product with input-side fee
          - swap invariant 改为基于真实 amount_in/out 校验
          - 非零 total_supply 的 add-liquidity mint 改为按 fee mint 后 total_supply 与现有 reserve 比例计算
          - `k_last` 不再在 swap 时刷新
          - remove_liquidity 也会结算 mint_fee 和更新 reserves

          结构收敛：
          - `validate_swap_invariant` / `mint_protocol_fee` / `update_k_last` 已从 StateInterface 顶层 helper
            收回到 swap/add/remove 主流程，只保留 Pool 纯数学函数
          - pool service 的 `totalSupply` / `liquidity(owner)` / `calculate_amount_liquidity`
            已按 pending protocol fee 稀释后的 effective total supply 返回

          已覆盖的验证点：
          - `totalSupply/liquidity(owner)` 已体现 pending fee 稀释
          - virtual initial liquidity 转为锁定初始 supply，不再把初始储备注入首个真实 LP
          - contract 回归覆盖初始 user liquidity=0、首次 add 后 user liquidity<total_supply、
            用户清空自身 LP 后池子仍保留锁定 supply/reserve
          - `NewTransaction` 历史写入开始修正为稳定 transaction_id，保留真实重复交易，同时对重复投递保持幂等

          未完项：
          - 核验线上坏历史是否来自旧版本遗留
          - 核验 async 闭环在 `NewTransaction` 前中断时是否会造成永久历史缺口
          - 当前环境的 Wasmer 集成测仍受 os error 12 限制，需要单独环境完成最终验证
  - name: Contract Tests
    tasks:
      - id: TEST-001
        status: Done
        priority: P1
        depends_on: []
        title: pool SetFeeTo / SetFeeToSetter 权限测试
        notes: 已覆盖 operator 正常路径 旧 operator 失效 非 operator 拒绝
      - id: TEST-002
        status: Done
        priority: P1
        depends_on: []
        title: pool RemoveLiquidity 集成级历史断言
        notes: 已覆盖 meme-native meme-meme 要求 latestTransactions 出现 RemoveLiquidity
      - id: TEST-003
        status: Done
        priority: P1
        depends_on: []
        title: pool min amount 边界测试
        notes: 已覆盖 Swap AddLiquidity RemoveLiquidity 刚好满足 差1attos 不满足
      - id: TEST-004
        status: Done
        priority: P0
        depends_on: []
        title: pool fund 异步闭环异常测试
        notes: 已覆盖第一笔成功第二笔失败 两笔成功但最终 AddLiquidity/NewTransaction 未闭环
      - id: TEST-005
        status: Done
        priority: P1
        depends_on: []
        title: pool NewTransaction 幂等与 5000 条队列边界测试
        notes: 已覆盖重复 message 4999/5000/5001 截断保序
      - id: TEST-006
        status: Done
        priority: P0
        depends_on: []
        title: swap UpdatePool 幂等/乱序测试
        notes: 已覆盖重复 transaction 旧 transaction 覆盖新状态 reserve/price 不回退
      - id: TEST-007
        status: Done
        priority: P1
        depends_on: []
        title: swap CreatePool / CreateUserPool / UserPoolCreated / PoolCreated 边界测试
        notes: 已覆盖重复建池 同 token 建池 错链回执 重复回执
      - id: TEST-008
        status: Done
        priority: P2
        depends_on: []
        title: proxy genesis miner remove 全链路测试
        notes: 已在多链集成层覆盖两阶段移除闭环
      - id: TEST-009
        status: Done
        priority: P2
        depends_on: []
        title: proxy miner register/deregister 边界测试
        notes: 已覆盖重复注册 未注册注销 跨 owner 错链 并修正静默 no-op 掩盖非法输入
      - id: TEST-010
        status: Done
        priority: P2
        depends_on: []
        title: proxy operator 治理测试
        notes: 已覆盖 AddOperator/BanOperator 两阶段治理 并修正 operator 校验与 ban 审批状态写回
      - id: TEST-011
        status: Done
        priority: P2
        depends_on: [TEST-010]
        title: proxy CreateMemeExt / MemeCreated 异步幂等测试
        notes: 已覆盖重复回执 错链回执 创建成功后状态写入 并修正 receipt 幂等处理
      - id: TEST-012
        status: Done
        priority: P1
        depends_on: []
        title: meme TransferFromApplication / InitializeLiquidity / TransferToCaller 权限测试
        notes: 已覆盖缺失 authenticated caller 错误 caller 重复初始化 余额边界
      - id: TEST-013
        status: Done
        priority: P1
        depends_on: []
        title: meme Mint / Redeem 边界测试
        notes: 已覆盖 Mint 非 owner signer 拒绝 Redeem(None) Redeem(Some(0)) 超额赎回
      - id: TEST-014
        status: Done
        priority: P2
        depends_on: []
        title: ams contract/integration 基础测试
        notes: 已覆盖 Register Claim AddApplicationType Update 正常 重复 权限 非法类型 并恢复 Claim 为 origin/runtime 当前发起者语义
      - id: TEST-015
        status: Done
        priority: P2
        depends_on: []
        title: blob-gateway contract/integration 基础测试
        notes: 已覆盖 Register operation 发消息 message 落库 重复 hash 幂等且保留首条 metadata
      - id: REV-001
        status: Done
        priority: P2
        depends_on: []
        title: Review 当前资金闭环与退款一致性风险
        notes: 已确认 3 类高风险缺口 AddLiquidity 双腿部分成功后失败无退款 pool payout/refund 吞掉 MemeResponse::Fail 初始建池路径吞掉下游失败仍持久化成功
  - name: Funds Consistency
    tasks:
      - id: FUND-001
        status: Doing
        priority: P0
        depends_on: []
        title: 定义跨链资金一致性协议边界与状态机
        notes: |
          先收口协议边界，再分实现。已确认：
          1. `transaction_id` 不连续本身不是异常；只有某笔已 `Reject` 或下游失败的交易仍残留
             reserve / total supply / share / 资金副作用，才算真实不一致
          2. Linera Wasm 合约里 native `transfer` 失败不可在合约内 catch，也拿不到可恢复的 fail 返回，
             因此不能依赖“payout fail 回执”修复
          3. 跨链消息无法原子，meme 输入链与 pool payout 链之间必须显式建模
             pending / success-only completion / user-claim or escrow 语义
          4. virtual liquidity、真实 reserve、可支付余额三者语义必须一并收口
          5. Linera `tracked + bouncing` 应作为原生的一跳 reject receipt 机制纳入协议设计，
             用于把“目标链 reject”可靠反馈回源链并收敛 `pending -> failed/refund-ready`；
             但它只覆盖单跳消息，不能替代多跳业务流程的显式状态机

          该任务输出协议定义：哪些阶段可持久化、哪些阶段只能 pending、哪些资金在成功前不得最终归
          pool 所有、哪些消息必须 tracked、bounce 回来后如何幂等收敛，以及 claim/refund/commit 的
          幂等边界。
      - id: FUND-002
        status: Todo
        priority: P0
        depends_on: [FUND-001, REV-001]
        title: 按统一协议修复 pool/swap 异步资金闭环
        notes: |
          合并原先分散 TODO，一次性覆盖高风险路径：
          - AddLiquidity 双腿部分成功后失败无退款
          - swap/remove/refund/payout 对下游 meme/native 发放缺少可靠闭环
          - 初始建池路径吞掉下游失败仍持久化成功
          - `NewTransaction` 或其他后续消息 `Reject` 后仍残留资金/状态副作用

          目标不是在合约内捕获 host transfer panic，而是让失败最多留下可验证的 pending，
          不留下错误 finalized 状态，并为成功路径补齐显式确认、幂等键与状态收敛。

          约束：
          - 优先利用 Linera `tracked + bouncing` 承接关键单跳消息的 reject 回执与源链收敛
          - 不能把它误当成全流程原子回滚
          - 后续 hop 的失败仍必须由业务协议状态机显式处理
      - id: FUND-003
        status: Todo
        priority: P0
        depends_on: [FUND-001]
        title: 明确 gas/funder 覆盖与链级运行保障
        notes: |
          `funder` OOM 已证明 chain gas 不足会直接打断任何可靠协议推进，这条线需要与资金一致性并行治理。
          需要定义必须持续充值的链集合与覆盖来源，确认包含预创建链和运行期新增链；
          并落实 `funder` 资源、链发现、监控告警与人工诊断接口，避免再次出现
          chain balance 耗尽但 application balance 仍在、表面看像资金一致性故障的情况。
      - id: FUND-004
        status: Todo
        priority: P1
        depends_on: [FUND-002]
        title: 为资金协议与失败收敛补回归测试
        notes: 覆盖部分成功退款、pending 不 finalize、重复 success/commit/claim 幂等、初始建池下游失败、Reject 后无残留副作用，以及 virtual liquidity 与真实可支付余额边界
  - name: WebUI
    tasks:
      - id: WEBUI-001
        status: Todo
        priority: P1
        depends_on: []
        title: 前端国际化
        notes: 梳理当前硬编码文案并建立可持续的 i18n 接入与迁移范围
      - id: WEBUI-002
        status: Todo
        priority: P1
        depends_on: []
        title: 优化 transactions 列表的拉取 加载与显示
        notes: 聚焦 transactions 列表的数据获取时机 加载态 空态 分页或增量展示与可读性
      - id: WEBUI-003
        status: Todo
        priority: P1
        depends_on: []
        title: 优化 FAQ
        notes: 聚焦 FAQ 的信息结构 文案质量 品牌一致性与版面表现
      - id: WEBUI-004
        status: Todo
        priority: P1
        depends_on: []
        title: 优化 Add Liquidity 和 Remove Liquidity 页面
        notes: 聚焦加减池页面的布局 信息层级 交互状态与易用性
      - id: WEBUI-005
        status: Todo
        priority: P1
        depends_on: []
        title: 实现侧边弹出菜单
        notes: 明确触发入口 展开行为 响应式表现与菜单项结构
