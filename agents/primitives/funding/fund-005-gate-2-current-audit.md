# FUND-005 Gate 2 Current Implementation Audit Table

Type: Primitive
Audience: Coding assistants
Authority: High

## Purpose

Record the `FUND-005` Gate 2 current implementation audit table. This table
uses the Gate 1E fixed columns and must stay within Gate 1A layers, Gate 1B
files, Gate 1C paths, and Gate 1D exclusions.

## Rules

- Do not add rows outside the Gate 1E column contract.
- Do not cite files outside Gate 1B as current implementation evidence.
- Do not cite Gate 1D excluded paths as current implementation evidence.
- Add rows path by path.
- Keep row ids stable after review.

## Current Implementation Audit Table

| id | layer | path | file | symbol | current_behavior | current_state_reads | current_state_writes | economic_effect | source_and_auth_checks | identity_and_amount_checks | terminal_truth | target_delta | required_tests | product_truth | status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| G2-CM-001 | Protocol definition layer | CreateMeme | abi/src/proxy.rs | primary: `ProxyOperation::CreateMeme`; direct message symbols: `ProxyMessage::CreateMeme`, `ProxyMessage::CreateMemeExt`, `ProxyMessage::MemeCreated` in `abi/src/proxy.rs` | Defines public create-meme operation and the three internal messages used by the current create-meme workflow. | none | none | none | missing source-chain check at ABI layer; missing authenticated-signer check at ABI layer; missing authenticated-caller check at ABI layer | carries `meme_instantiation_argument`, `meme_parameters`, `bytecode_id`, `chain_id`, and `token`; missing workflow identity | missing | target requires explicit terminal workflow truth for failed, bounced, stale, and partially completed create-meme workflows | current tests: `proxy/src/contract_tests.rs`, `proxy/src/contract_tests/contract.rs`, `proxy/tests/single_owner_real.rs`, `proxy/tests/single_owner_virtual.rs`, `proxy/tests/multi_owner.rs`, `proxy/tests/suite.rs`; missing characterization test for create-meme bounced `ProxyMessage::CreateMeme`; missing characterization test for stale `ProxyMessage::MemeCreated` without prior `state.create_chain` | none | current_gap, target_gap, test_gap |
| G2-CM-002 | Contract handler layer | CreateMeme | proxy/src/contract_inner/handlers/operation/create_meme.rs | primary: `CreateMemeHandler::handle`; direct protocol entry: `ProxyOperation::CreateMeme` in `abi/src/proxy.rs` | Copies proxy app id into meme instantiation argument, copies virtual and initial liquidity into meme argument, funds proxy creator chain, sets `meme_parameters.creator` from authenticated account, and sends `ProxyMessage::CreateMeme` to the proxy application creator chain. | runtime `application_id()`, `application_creator_chain_id()`, `authenticated_signer()`, `authenticated_account()` | outgoing `ProxyMessage::CreateMeme`; runtime `transfer_combined(None, proxy_creator_chain AccountOwner::CHAIN, open_chain_fee_budget())`; runtime `transfer_combined(None, proxy_creator_chain signer, open_chain_fee_budget())` when `initial_liquidity` exists; runtime `transfer_combined(None, proxy_creator_chain signer, liquidity.native_amount)` when `virtual_initial_liquidity == false` and `initial_liquidity` exists | fee-budget funding; native transfer; outgoing message | authenticated account is read for creator; authenticated signer is unwrapped in the pool-chain fee funding branch and in the non-virtual native liquidity funding branch; missing explicit source-chain check; missing workflow identity | copies operation liquidity fields; checks `initial_liquidity.is_some()` for pool-chain fee funding; checks `virtual_initial_liquidity == false` before native liquidity funding; missing amount overflow terminal handling beyond runtime arithmetic behavior | missing | target requires intent state and terminal workflow state before funding and outgoing message can be considered closed | current tests: `proxy/src/contract_tests.rs`, `proxy/src/contract_tests/contract.rs`, `proxy/tests/single_owner_real.rs`, `proxy/tests/single_owner_virtual.rs`; missing characterization test for signer absence when `initial_liquidity` exists; missing characterization test for operation funding movement amounts | none | current_gap, target_gap, test_gap |
| G2-CM-003 | Contract handler layer | CreateMeme | proxy/src/contract_inner/handlers/message/create_meme.rs | primary: `CreateMemeHandler::handle`; callee: `CreateMemeHandler::create_meme_chain` in `proxy/src/contract_inner/handlers/message/create_meme.rs`; direct protocol entry: `ProxyMessage::CreateMeme` in `abi/src/proxy.rs` | On proxy creator chain, sets swap application id, opens meme chain with permissions, funds newly opened meme chain when computed funding amount is positive, sends `ProxyMessage::CreateMemeExt` to the new chain, and records the created chain in proxy state. | state `swap_application_id()`, `miners()`, `is_genesis_miner(...)`, `meme_bytecode_id()`; runtime `application_id()`, `open_chain(...)`, `authenticated_signer()`, `owner_balance(signer)`, `system_time()` | runtime `open_chain(...)`; runtime `transfer(signer, Account { chain_id: meme_chain_id, owner: signer }, amount)` when computed amount is positive; outgoing `ProxyMessage::CreateMemeExt`; state `create_chain(chain_id, system_time)` | chain opening; permission change through open-chain permissions; fee-budget funding; native transfer; outgoing message; chain-created state fact | authenticated signer is read before zero-amount return in `fund_meme_chain_initial_liquidity(...)`; owner balance is read before zero-amount return; balance assert runs only when computed amount is positive; missing explicit message-origin check; missing authenticated-caller application check; missing workflow identity | amount starts at `open_chain_fee_budget()` when `initial_liquidity` exists; adds `liquidity.native_amount` when `virtual_initial_liquidity == false`; no persisted transfer id; no intent id | partial terminal truth in `state.create_chain`; missing terminal truth for failed open-chain follow-up, bounced `CreateMemeExt`, stale message, and partial completion | target requires unique workflow terminal state and bounce-aware failure convergence | current tests: `proxy/src/contract_tests.rs`, `proxy/src/contract_tests/contract.rs`, `proxy/tests/single_owner_real.rs`, `proxy/tests/single_owner_virtual.rs`; missing characterization test for zero funding amount still requiring authenticated signer; missing characterization test for duplicate `ProxyMessage::CreateMeme` on creator chain; missing characterization test for `CreateMemeExt` bounce after `state.create_chain` | proxy state is contract state truth for chain creation; product projection truth not established in this row | current_gap, target_gap, test_gap |
| G2-CM-004 | Persistent state layer | CreateMeme | proxy/src/state/state_impl.rs | primary: `ProxyState::create_chain`; interface symbol: `StateInterface::create_chain` in `proxy/src/interfaces/state.rs` | Inserts `Chain { chain_id, created_at, token: None }` into proxy `chains`. | none | proxy `chains[chain_id] = Chain { chain_id, created_at, token: None }` | chain-created state fact | missing source-chain check at state method; caller layer is responsible | missing duplicate-chain guard beyond map insert replacement semantics | `chains[chain_id]` with `token: None` is partial truth for opened chain; final meme token truth is missing until `create_chain_token` | target requires terminal truth not split across partial chain-created truth and later token-created truth without workflow status | current tests: `proxy/src/contract_tests.rs`, `proxy/src/contract_tests/contract.rs`; missing characterization test for duplicate `create_chain` with same `chain_id` before `MemeCreated` | contract state truth | current_gap, target_gap, test_gap |
| G2-CM-005 | Contract handler layer | CreateMeme | proxy/src/contract_inner/handlers/message/create_meme_ext.rs | primary: `CreateMemeExtHandler::handle`; callee: `CreateMemeExtHandler::create_meme_application` in `proxy/src/contract_inner/handlers/message/create_meme_ext.rs`; direct protocol entry: `ProxyMessage::CreateMemeExt` in `abi/src/proxy.rs` | On meme chain, creates meme application, changes application permissions to the created meme application, and sends `ProxyMessage::MemeCreated` to proxy creator chain with meme chain id and application id. | runtime `create_application(...)`, `application_id()`, `change_application_permissions(...)`, `chain_id()`, `application_creator_chain_id()` | created meme application; runtime `change_application_permissions(...)`; outgoing `ProxyMessage::MemeCreated` | application creation; permission change; outgoing message | missing source-chain check; missing authenticated-caller check; missing workflow identity; no bounce handling | carries `bytecode_id`, instantiation argument, parameters, produced `chain_id`, produced `token`; missing intent id | missing until proxy creator chain receives `MemeCreated`; no local terminal failure truth for failed permission change beyond panic | target requires bounce-aware completion and failure convergence for meme application creation workflow | current tests: `proxy/src/contract_tests.rs`, `proxy/src/contract_tests/contract.rs`, `proxy/tests/single_owner_real.rs`, `proxy/tests/single_owner_virtual.rs`; missing characterization test for duplicate `CreateMemeExt`; missing characterization test for permission-change failure behavior | none | current_gap, target_gap, test_gap |
| G2-CM-006 | Contract handler layer | CreateMeme | proxy/src/contract_inner/handlers/message/meme_created.rs | primary: `MemeCreatedHandler::handle`; direct protocol entry: `ProxyMessage::MemeCreated` in `abi/src/proxy.rs` | Consumes `ProxyMessage::MemeCreated` and writes meme token application id onto the previously created proxy chain record. | message `chain_id`, message `token` | state `create_chain_token(chain_id, token)` | token-created state fact | missing message-origin check; missing authenticated-caller check; missing expected chain/workflow validation | validates neither token application identity nor workflow identity; state layer handles missing chain as no-op and token conflict as assert | `chains[chain_id].token = Some(token)` is current terminal token truth when chain exists; missing terminal truth when chain is absent because state returns `Ok(())` | target requires stale `MemeCreated` to be explicit terminal failure or rejected stale receipt, not silent no-op | current tests: `proxy/src/contract_tests.rs`, `proxy/src/contract_tests/contract.rs`; missing characterization test for `MemeCreated` with unknown chain id; missing characterization test for conflicting token on same chain id | proxy state is contract state truth for created meme token; product projection truth not established in this row | current_gap, target_gap, test_gap |
| G2-CM-007 | Persistent state layer | CreateMeme | proxy/src/state/state_impl.rs | primary: `ProxyState::create_chain_token`; interface symbol: `StateInterface::create_chain_token` in `proxy/src/interfaces/state.rs` | Loads chain by id, returns `Ok(())` when chain is absent, returns `Ok(())` when existing token equals incoming token, asserts when existing token differs, and writes token when token is absent. | proxy `chains[chain_id]` | proxy `chains[chain_id].token = Some(token)` when chain exists and token is absent | token-created state fact | missing source-chain check at state method; caller layer is responsible | idempotent for same token; assert for conflicting token; stale unknown chain silently succeeds | terminal truth exists only for known chain id with token set; missing terminal truth for unknown chain id stale receipt | target requires stale receipt visibility and unique workflow terminal status | current tests: `proxy/src/contract_tests.rs`, `proxy/src/contract_tests/contract.rs`; missing characterization test for absent chain no-op; missing characterization test for conflicting token assert | contract state truth | current_gap, target_gap, test_gap |
| G2-CM-008 | Contract dispatch boundary layer | CreateMeme | proxy/src/contract_impl.rs | primary: `ProxyContract::on_op`; callee: `ProxyContract::on_message` in `proxy/src/contract_impl.rs`; runtime send symbol: `ContractRuntimeContext::send_message` in `runtime/src/interfaces/contract.rs` | Dispatches operation and message handlers through `HandlerFactory`, drains `HandlerOutcome.messages`, and sends each outgoing proxy message through runtime `send_message`. | handler outcome messages; runtime context | outgoing runtime messages | outgoing message dispatch | dispatch layer does not add workflow tracking metadata; runtime adapter adds authentication; no bounce dispatch entry in this file | no intent id, transfer id, workflow id added at dispatch | missing for bounced messages; panics on handler error | target requires tracked send and bounced-message convergence for funding workflows | current tests: `proxy/src/contract_tests.rs`, `proxy/src/contract_tests/contract.rs`; missing characterization test proving current tracking behavior for create-meme outgoing messages; missing characterization test for bounced proxy message dispatch | none | current_gap, target_gap, test_gap |
| G2-MR-001 | Protocol definition layer | Meme Mining / Redeem | abi/src/meme.rs | primary: `MemeOperation::Mine`; direct operation symbol: `MemeOperation::Redeem` in `abi/src/meme.rs`; direct message symbol: `MemeMessage::Redeem` in `abi/src/meme.rs` | Defines mining as a public operation, redeem as a public operation, and redeem follow-up as an internal message carrying owner and optional amount. | none | none | none | missing source-chain check at ABI layer; missing authenticated-signer check at ABI layer; missing authenticated-caller check at ABI layer | `Mine` carries nonce only; `Redeem` carries optional amount; `MemeMessage::Redeem` carries owner and optional amount; missing workflow identity | missing | current `Redeem` is meme-local balance exit; target pool `Claim` is the only user-facing funds-exit operation for pool-held value; meme `Redeem` does not create pool claim balance | current tests: `meme/src/contract_tests.rs`, `meme/src/contract_tests/contract.rs`, `meme/tests/meme_mining_full_supply.rs`, `meme/tests/meme_mining_part_supply.rs`, `meme/tests/meme_no_mining.rs`; missing characterization test for redeem message with absent amount; missing characterization test for redeem message with stale owner chain context | none | current_gap, target_gap, test_gap |
| G2-MR-002 | Contract handler layer | Meme Mining / Redeem | meme/src/contract_inner/handlers/operation/mine.rs | primary: `MineHandler::handle`; callee: `MineHandler::verify` in `meme/src/contract_inner/handlers/operation/mine.rs` | Requires mining to be enabled, validates height, signer, previous nonce, and target hash, updates mining info, then mints reward to the authenticated account. | runtime `enable_mining()`, `block_height()`, `chain_id()`, `authenticated_signer()`, `authenticated_account()`, `system_time()`; state `mining_height()`, `previous_nonce()`, `mining_target()`, `mining_info()` | state `update_mining_info(...)`; state `mining_reward(owner, now)` | meme mint; mined token balance increase | authenticated signer is required in verification; authenticated account receives reward; missing explicit first-operation-in-block check despite code TODO | hash must be less than or equal to mining target; height must be at least mined height; nonce becomes previous nonce; reward amount comes from state mining info | mining info is current truth for height, nonce, target, reward amount, and mining progress; no claim terminal truth participates | target keeps mining reward as meme token balance accrual; pool claim balance is not involved in mining reward accrual | current tests: `meme/tests/meme_mining_full_supply.rs`, `meme/tests/meme_mining_part_supply.rs`, `meme/tests/meme_no_mining.rs`; missing characterization test for missing signer panic; missing characterization test for stale height rejection; missing characterization test for mining reward recipient account | contract state truth | current_gap, target_gap, test_gap |
| G2-MR-003 | Contract handler layer | Meme Mining / Redeem | meme/src/contract_inner/handlers/operation/redeem.rs | primary: `RedeemHandler::handle`; direct protocol entry: `MemeOperation::Redeem` in `abi/src/meme.rs` | Reads authenticated account as owner and sends `MemeMessage::Redeem { owner, amount }` to the meme application creator chain. | runtime `authenticated_account()`, `application_creator_chain_id()` | outgoing `MemeMessage::Redeem` | outgoing message dispatch for meme balance exit | authenticated account is the message owner; missing explicit source-chain check; missing workflow identity | amount is copied from operation payload; absent amount remains absent until creator-chain message handling | missing | `MemeOperation::Redeem` remains a meme-local balance-exit operation and does not create pool claim balance; pool `Claim` is the single user-facing funds-exit operation for pool-held value | current tests: `meme/src/contract_tests.rs`, `meme/src/contract_tests/contract.rs`; missing characterization test for authenticated account source; missing characterization test for absent amount payload propagation | none | current_gap, target_gap, test_gap |
| G2-MR-004 | Contract handler layer | Meme Mining / Redeem | meme/src/contract_inner/handlers/message/redeem.rs | primary: `RedeemHandler::handle`; direct protocol entry: `MemeMessage::Redeem` in `abi/src/meme.rs` | On creator chain, constructs source as current chain id plus `owner.owner`, selects explicit amount when present, selects full source balance when amount is absent, and transfers meme balance from source to owner. | runtime `chain_id()`; state `balance_of(from)` when amount is absent | state `transfer(from, owner, amount)` | meme transfer; meme-chain balance exit | missing message-origin check; missing authenticated-caller check; missing proof that source balance belongs to message sender | source account uses current chain id and `owner.owner`; amount must pass `state.transfer` positive and sufficient-balance assertions | successful `state.transfer` balance mutation is current terminal truth; missing fail, bounced, stale, and pending truth for redeem message | redeem message completion is meme balance mutation only; pool claim and claiming balances are outside this state transition | current tests: `meme/src/contract_tests.rs`, `meme/src/contract_tests/contract.rs`; missing characterization test for absent amount full-balance transfer; missing characterization test for amount above source balance; missing characterization test for wrong message origin | contract state truth | current_gap, target_gap, test_gap |
| G2-MR-005 | Persistent state layer | Meme Mining / Redeem | meme/src/state/state_impl.rs | primary: `MemeState::mining_reward`; callee: `MemeState::mint` in `meme/src/state/state_impl.rs`; callee: `MemeState::transfer` in `meme/src/state/state_impl.rs` | Mining reward reads current reward amount, mints from holder to owner, halves and adjusts mining info when thresholds are met, and writes updated mining info. Transfer asserts positive amount, non-self transfer, and sufficient balance before moving balances. | `mining_info.reward_amount`, `mining_info` fields, holder balance, sender balance | balances map; mining info | meme mint; meme transfer | state methods do not validate message source; caller layer owns source validation | amount positivity and sufficient balance checks exist in `transfer`; reward amount comes from mining info with no per-reward cap check at handler layer | balances and mining info are current terminal truth | meme state contains balances and mining info; target pool claim and claiming balances are not part of meme mining state | current tests: `meme/tests/meme_mining_full_supply.rs`, `meme/tests/meme_mining_part_supply.rs`; missing characterization test for reward amount halving transition; missing characterization test for redeem transfer self-transfer rejection | contract state truth | current_gap, target_gap, test_gap |
| G2-MIL-001 | Contract handler layer | Meme InitializeLiquidity | meme/src/contract_inner/instantiation_handler.rs | primary: `InstantiationHandler::instantiate`; callee: `InstantiationHandler::create_liquidity_pool` in `meme/src/contract_inner/instantiation_handler.rs` | Instantiation initializes meme state, mints initial owner balance, calls state initialize-liquidity with runtime liquidity, then `create_liquidity_pool()` re-reads runtime `initial_liquidity()` for funding checks and native transfer amount instead of reading adjusted state `_initial_liquidity()`. It funds swap creator chain fee budget, optionally funds native liquidity, and sends `MemeMessage::LiquidityFunded`. | runtime `creator_signer()`, `authenticated_signer()`, `creator()`, `application_account()`, `virtual_initial_liquidity()`, `initial_liquidity()`, `enable_mining()`, `mining_supply()`, `system_time()`, `application_creator_chain_id()`, `swap_creator_chain_id()`; state `initial_owner_balance()`, `swap_application_id()` | state `instantiate(...)`; state `mint(...)`; state `initialize_liquidity(...)`; runtime `transfer_combined(None, swap_creator_chain AccountOwner::CHAIN, open_chain_fee_budget())`; runtime `transfer_combined(None, swap_application_account, runtime liquidity.native_amount)` when virtual initial liquidity is false; outgoing `MemeMessage::LiquidityFunded` | meme mint; allowance custody; fee-budget funding; native transfer; outgoing message | requires authenticated signer equals creator signer; missing explicit post-funding completion boundary for initial-liquidity path | skips liquidity-pool funding when swap application id, runtime initial liquidity, runtime fungible amount, or runtime native amount is absent; native funding amount comes from runtime liquidity while meme allowance can come from mining-adjusted state liquidity | missing for failed, bounced, duplicate, stale, and partially completed initialize-liquidity workflow | target requires instantiate/app creation, funds-enter-pool, and `FinalizeInitialization` to be separated; current implementation mixes pre-create funding, later swap->pool transfer, and early economics write | current tests: `meme/src/contract_tests.rs`, `meme/src/contract_tests/contract.rs`, `swap/tests/meme_native_pair_mining_full_supply.rs`, `swap/tests/meme_native_pair_mining_part_supply.rs`, `swap/tests/meme_native_pair_no_mining.rs`; missing characterization test for runtime liquidity exceeding mining-adjusted state liquidity; missing characterization test for zero adjusted fungible liquidity with positive runtime liquidity; missing characterization test for `LiquidityFunded` bounce | none | current_gap, target_gap, test_gap |
| G2-MIL-002 | Persistent state layer | Meme InitializeLiquidity | meme/src/state/state_impl.rs | primary: `MemeState::initialize_liquidity`; callee: `MemeState::instantiate` in `meme/src/state/state_impl.rs` | Instantiation writes swap, holder, owner, meme, blob, AMS, proxy, and mining state. Initialize-liquidity caps fungible liquidity by mining supply, writes adjusted liquidity, and approves swap application account for adjusted fungible liquidity. | holder balance; runtime-supplied liquidity; mining enablement; mining supply; `swap_application_id` | `initial_liquidity`; balances; allowances; meme metadata; owner; holder; mining info | meme allowance custody; initial supply placement | state method trusts caller for creator and swap creator chain; no source-chain validation inside state | holder balance must cover requested fungible liquidity and mining supply; adjusted fungible liquidity can become zero and return without writing liquidity | initial liquidity and allowance are partial truth; terminal workflow truth is missing | target requires later pool-side `FinalizeInitialization` after swap/meme funding reaches the pool application; virtual semantics need a non-withdrawable invariant rather than a persisted initialization intent | current tests: `meme/src/contract_tests.rs`, `meme/tests/meme_mining_full_supply.rs`; missing characterization test for mining cap adjustment; missing characterization test for approve owner and spender accounts | contract state truth | current_gap, target_gap, test_gap |
| G2-MIL-003 | Contract handler layer | Meme InitializeLiquidity | meme/src/contract_inner/handlers/message/liquidity_funded.rs | primary: `LiquidityFundedHandler::handle`; direct protocol entry: `MemeMessage::LiquidityFunded` in `abi/src/meme.rs`; direct call payload: `SwapOperation::InitializeLiquidity` in `abi/src/swap/router.rs` | Reads state initial liquidity and swap app id, builds `SwapOperation::InitializeLiquidity`, and calls swap application with creator, token application, token chain, amounts, and virtual-liquidity flag. | runtime `virtual_initial_liquidity()`, `creator()`, `chain_id()`, `application_id()`; state `_initial_liquidity()`, `swap_application_id()` | runtime `call_application(swap_application_id, InitializeLiquidity)` | cross-application call; initialization continuation | missing message-origin check; missing authenticated-caller check for `LiquidityFunded`; no bounce handling for the later swap message chain | token id comes from current meme application id; amount source comes from adjusted state liquidity; virtual flag comes from runtime parameters | missing | target requires source validation and a later split between pool app creation, real-funding entry into the pool, and post-funding `FinalizeInitialization`; `LiquidityFunded` is a readiness continuation, not a completion boundary | current tests: `swap/tests/meme_native_pair_mining_full_supply.rs`, `swap/tests/meme_native_pair_no_mining.rs`; missing characterization test for absent state liquidity no-op; missing characterization test for absent swap app no-op; missing characterization test for rejected swap call | none | current_gap, target_gap, test_gap |
| G2-MIL-004 | Contract handler layer | Meme InitializeLiquidity | swap/src/contract_inner/handlers/operation/initialize_liquidity.rs | primary: `InitializeLiquidityHandler::handle`; callee: `InitializeLiquidityHandler::formalize_virtual_liquidity` in `swap/src/contract_inner/handlers/operation/initialize_liquidity.rs`; direct protocol entry: `SwapOperation::InitializeLiquidity` in `abi/src/swap/router.rs` | Requires authenticated caller id equals token0 and current chain equals token0 creator chain, then calls `formalize_virtual_liquidity(...)`. Invalid virtual-liquidity conditions are normalized to `false` instead of causing rejection, and the handler sends `SwapMessage::InitializeLiquidity` to swap creator chain. | runtime `authenticated_caller_id()`, `chain_id()`, `application_creator_chain_id()` | outgoing `SwapMessage::InitializeLiquidity` | outgoing message dispatch | requires authenticated caller id equals token_0; requires current chain id equals token_0 creator chain id | virtual liquidity is true only when requested, token1 is native, caller application is token0, and current chain is token0 creator chain; invalid virtual-liquidity request becomes false; missing persisted initialization intent id | missing | target requires initialization intent binding and explicit virtual-liquidity truth instead of silent invalid-to-false normalization | current tests: `swap/tests/meme_native_pair_no_mining.rs`, `swap/tests/meme_native_pair_mining_full_supply.rs`; missing characterization test for invalid virtual-liquidity request becoming false; missing characterization test for wrong authenticated caller; missing characterization test for wrong creator chain | none | current_gap, target_gap, test_gap |
| G2-MIL-004A | Contract handler layer | Meme InitializeLiquidity | swap/src/contract_inner/handlers/message/initialize_liquidity.rs | primary: `InitializeLiquidityHandler::handle`; callee: `CreatePoolHandler::handle` in `swap/src/contract_inner/handlers/create_pool.rs`; direct protocol entry: `SwapMessage::InitializeLiquidity` in `abi/src/swap/router.rs` | On swap creator chain, delegates meme-native initialization to the shared create-pool handler with `token_1 = None`, `virtual_liquidity` copied from message, and `user_pool = false`. | message `creator`, `token_0`, `amount_0`, `amount_1`, `virtual_liquidity`, `to` | delegated `state.create_pool_chain(destination)`; delegated outgoing `SwapMessage::CreatePool` | chain opening; permission configuration; outgoing message | missing message-origin check; missing authenticated-caller check; no persisted initialization workflow boundary | amount and virtual-liquidity identity are trusted from message; no workflow id | missing | target requires create-pool to produce an uninitialized pool app, followed by real-funding entry into the pool and post-funding `FinalizeInitialization`; `PoolCreated` is app-created only | current tests: `swap/tests/meme_native_pair_no_mining.rs`; missing characterization test for stale `SwapMessage::InitializeLiquidity`; missing characterization test for wrong message origin | none | current_gap, target_gap, test_gap |
| G2-MIL-005 | Contract handler layer | Meme InitializeLiquidity | swap/src/contract_inner/handlers/message/pool_created.rs | primary: `PoolCreatedHandler::handle`; callee: `PoolCreatedHandler::initial_pool_created` in `swap/src/contract_inner/handlers/message/pool_created.rs` | For non-user initial pool creation, verifies pool chain is tracked, performs native transfer when virtual liquidity is false, calls meme `InitializeLiquidity`, then writes swap pool truth. | state `is_pool_chain(...)`, `get_pool_exchangable(...)`; runtime `application_id()`, `system_time()` | runtime `transfer(application, pool_application, amount_1)` when non-virtual; runtime `call_application(token_0, MemeOperation::InitializeLiquidity)`; state `create_pool(...)` | native transfer; cross-application call; swap pool truth | verifies tracked pool chain; missing message-origin check; missing authenticated-caller check; duplicate same pool receipt returns no-op | requires positive amounts; checks no existing exchangeable pool except duplicate same pool | swap pool state is success truth; missing terminal truth for failed meme initialization after `create_pool` | target requires `PoolCreated` to remain app-created only, later real-funding entry into the pool application, and final usable truth only after post-funding `FinalizeInitialization` | current tests: `swap/tests/meme_native_pair_no_mining.rs`, `swap/tests/meme_native_pair_without_initial_liquidity.rs`; missing characterization test for `UpdatePool` before `create_pool`; missing characterization test for meme initialize-liquidity failure after swap pool truth | contract state truth; projection truth follows later transaction facts | current_gap, target_gap, test_gap |
| G2-MIL-006 | Contract handler layer | Meme InitializeLiquidity | meme/src/contract_inner/handlers/message/initialize_liquidity.rs | primary: `InitializeLiquidityHandler::handle`; direct protocol entry: `MemeMessage::InitializeLiquidity` in `abi/src/meme.rs` | On meme creator chain, requires caller to be the swap application on swap creator chain, transfers approved meme liquidity from meme application creation account to pool account, then changes ownership through `OpenMultiLeaderRoundsHandler`. | runtime `swap_creator_chain_id()`, `application_creation_account()`; state `swap_application_id()` | state `transfer_from(caller, from, to, amount)`; ownership change via handler | meme transfer; ownership change | requires caller chain equals swap creator chain; requires caller owner equals swap application id | amount must be covered by allowance; `transfer_from` asserts allowance; missing workflow id | transfer and ownership mutation are current terminal truth for this step; missing full workflow terminal truth | target requires source, app, amount, and workflow binding across meme, swap, and pool | current tests: `meme/src/contract_tests.rs`, `swap/tests/meme_native_pair_mining_full_supply.rs`; missing characterization test for wrong caller owner; missing characterization test for insufficient allowance | contract state truth | current_gap, target_gap, test_gap |
| G2-MIL-007 | Contract dispatch boundary layer | Meme InitializeLiquidity | pool/src/contract_impl.rs | primary: `PoolContract::_instantiate`; direct state symbol: `PoolState::instantiate` in `pool/src/state/state_impl.rs`; direct protocol entry: `PoolMessage::NewTransaction` in `abi/src/swap/pool/mod.rs` | Pool instantiation writes pool state and, when both instantiation amounts are positive, builds an initial-liquidity transaction and sends `PoolMessage::NewTransaction` to the current pool chain. | runtime `application_parameters()`, `creator()`, `system_time()`, `chain_id()`; state `instantiate(...)` return liquidity | pool state instantiate writes; transaction built through state; outgoing `PoolMessage::NewTransaction` | initial LP mint; reserve mutation; transaction fact; outgoing message | instantiation trust comes from application creation; no workflow intent validation in pool dispatch | sends no transaction when either instantiation amount is zero; transaction owner is pool creator | pool state and transaction message are current economic truth; full initialize-liquidity terminal truth is missing | target requires initial-liquidity finalization to converge through the reviewed message-driven funding path before router pool truth becomes active | current tests: `pool/src/contract_tests.rs`, `pool/tests/meme_native_pool.rs`; missing characterization test for initial pool instantiation transaction emission; missing characterization test for zero amount user-pool instantiation emitting no transaction | contract state truth; projection truth follows `NewTransaction` | current_gap, target_gap, test_gap |
| G2-MIL-008 | Contract handler layer | Meme InitializeLiquidity | pool/src/contract_inner/handlers/message/new_transaction.rs | primary: `NewTransactionHandler::handle`; direct call payload: `SwapOperation::UpdatePool` in `abi/src/swap/router.rs` | Reads pool prices and reserves, builds `SwapOperation::UpdatePool`, and calls router application. | state `calculate_price_pair()`, `reserve_0()`, `reserve_1()`, `router_application_id()`; runtime `token_0()`, `token_1()` | runtime `call_application(router, SwapOperation::UpdatePool)` | transaction propagation; router update call | missing message-origin check; missing authenticated-caller check | transaction payload is trusted from message; no workflow id | missing | target requires update propagation to be bound to finalized pool workflow and to expose stale update behavior | current tests: `pool/src/contract_tests.rs`; missing characterization test for stale `PoolMessage::NewTransaction`; missing characterization test for router update call payload | none | current_gap, target_gap, test_gap |
| G2-MIL-009 | Contract handler layer | Meme InitializeLiquidity | swap/src/contract_inner/handlers/message/update_pool.rs | primary: `UpdatePoolHandler::handle`; direct protocol entry: `SwapMessage::UpdatePool` in `abi/src/swap/router.rs` | Writes router pool transaction, price, and reserve truth through `state.update_pool(...)` without checking whether router pool truth already exists. | message `token_0`, `token_1`, `transaction`, `token_0_price`, `token_1_price`, `reserve_0`, `reserve_1` | state `update_pool(...)` | router pool truth update; transaction projection input | missing message-origin check; missing authenticated-caller check | no pool application identity, workflow id, or active-pool precondition is checked in handler | router state update is current product-facing pool truth; stale update terminal truth is missing | target requires update-pool receipt to bind to expected active pool and workflow state | current tests: `swap/src/contract_tests.rs`; missing characterization test for `UpdatePool` before `state.create_pool`; missing characterization test for stale update after competing pool creation | contract state truth; projection truth after normalization | current_gap, target_gap, test_gap |
| G2-UCP-001 | Protocol definition layer | User CreatePool With Initial Liquidity | abi/src/swap/router.rs | primary: `SwapOperation::CreatePool`; direct message symbols: `SwapMessage::CreateUserPool`, `SwapMessage::CreatePool`, `SwapMessage::PoolCreated`, `SwapMessage::UserPoolCreated` in `abi/src/swap/router.rs` | Defines public create-pool operation and the internal messages used for authoritative pair validation, pool app creation, app-created receipt handling, and first funding kickoff. | none | none | none | missing source-chain check at ABI layer; missing signer check at ABI layer | carries token ids, amounts, recipient, and later-hop continuation facts; missing explicit origin field where later hops need the initial operation account | missing | target keeps create-pool as a message-driven workflow with no persisted create-pool intent; pool app creation, first real funding, and `FinalizeInitialization` are distinct protocol steps | current tests: `swap/src/contract_tests.rs`, `swap/tests/meme_meme_pair.rs`; missing characterization test for zero amount create-pool rejection; missing characterization test for concurrent same-pair create-pool contention | none | current_gap, target_gap, test_gap |
| G2-UCP-002 | Contract handler layer | User CreatePool With Initial Liquidity | swap/src/contract_inner/handlers/operation/create_pool.rs | primary: `CreatePoolHandler::handle`; direct protocol entry: `SwapOperation::CreatePool` in `abi/src/swap/router.rs` | Requires distinct tokens, reads authenticated signer, funds swap creator chain fee budget, resolves meme token creator chains, and sends `SwapMessage::CreateUserPool` to swap creator chain. | runtime `authenticated_signer()`, `owner_balance(...)`, `chain_balance()`, `application_creator_chain_id()`, `token_creator_chain_id(...)` | runtime `transfer(...)` for fee-budget funding; outgoing `SwapMessage::CreateUserPool` | fee-budget funding; outgoing message | authenticated signer is required; missing explicit authenticated account owner binding in payload | checks token inequality; does not require both amounts positive at operation entry; validates token creator chain through runtime call | missing | target requires fail-fast amount checks, authoritative-chain validation to stay on the later swap-creator-chain hop, and message-carried origin/funding facts instead of persisted intent state | current tests: `swap/src/contract_tests.rs`, `swap/tests/meme_meme_pair.rs`; missing characterization test for one-sided amount; missing characterization test for fee-budget movement when pool already exists | none | current_gap, target_gap, test_gap |
| G2-UCP-003 | Contract handler layer | User CreatePool With Initial Liquidity | swap/src/contract_inner/handlers/message/create_user_pool.rs | primary: `CreateUserPoolHandler::handle`; callee: `CreatePoolHandler::handle` in `swap/src/contract_inner/handlers/create_pool.rs` | On swap creator chain, rejects existing active pool by panic, derives creator from message signer account, resolves token creator chains, and delegates shell creation with `user_pool = true`. | state `get_pool_exchangable(...)`; runtime `message_signer_account()`, `token_creator_chain_id(...)` | delegated runtime `open_chain(...)`; delegated state `create_pool_chain(...)`; outgoing `SwapMessage::CreatePool` | chain opening; permission configuration; outgoing message | creator comes from message signer account; missing source-chain check; missing tracked bounce handling | checks token inequality and active-pool existence; missing positive amount check | missing for failed and bounced workflow; existing-pool branch panics without refund truth | target requires authoritative pair-existence validation on this hop, then creation of an uninitialized zero-economic pool app through pure message continuation with no persisted create-pool workflow state | current tests: `swap/src/contract_tests.rs`; missing characterization test for existing pool fee refund absence; missing characterization test for wrong message signer | none | current_gap, target_gap, test_gap |
| G2-UCP-004 | Contract handler layer | User CreatePool With Initial Liquidity | swap/src/contract_inner/handlers/message/pool_created.rs | primary: `PoolCreatedHandler::handle`; direct protocol entry: `SwapMessage::PoolCreated` in `abi/src/swap/router.rs` | Verifies pool chain is tracked, requires positive amounts, ignores duplicate same pool receipt, creates swap pool truth, and sends `UserPoolCreated` to creator chain when `user_pool` is true. | state `is_pool_chain(...)`, `get_pool_exchangable(...)`; runtime `system_time()` | state `create_pool(...)`; outgoing `SwapMessage::UserPoolCreated` | pool truth creation; outgoing message | tracked pool-chain check exists; missing message-origin check; missing authenticated-caller check | positive amount check exists at receipt stage; duplicate same pool receipt is no-op; different existing pool panics | swap pool state is success truth before user liquidity is funded | target requires `PoolCreated` to mean app-created receipt only, protocol pair existence to be written on this receipt, and pool usability to remain blocked until pool-side first funding converges through `FinalizeInitialization` | current tests: `swap/tests/meme_meme_pair.rs`; missing characterization test proving pool truth is written before user liquidity funding; missing characterization test for duplicate `PoolCreated` | contract state truth | current_gap, target_gap, test_gap |
| G2-UCP-005 | Contract handler layer | User CreatePool With Initial Liquidity | swap/src/contract_inner/handlers/message/user_pool_created.rs | primary: `UserPoolCreatedHandler::handle`; direct protocol entry: `SwapMessage::UserPoolCreated` in `abi/src/swap/router.rs`; direct call payload: `PoolOperation::AddLiquidity` in `abi/src/swap/pool/mod.rs` | Marks the pool application as processed, returns no-op when already processed, converts pool application account owner into application id, and calls pool `AddLiquidity` with user-provided amounts. | state `mark_user_pool_created(pool_application)` | state processed-user-pool marker; runtime `call_application(pool_application, PoolOperation::AddLiquidity)` | cross-application call; initial-liquidity funding kickoff | missing message-origin check; missing authenticated-caller check; processed marker is a late duplicate guard | requires pool application owner to be address32; amount positivity is deferred to pool add-liquidity operation | processed marker is duplicate truth; pool funding terminal truth is separate and missing | target removes the processed marker, keeps first funding as a pure message-driven kickoff into pool `AddLiquidity`, and treats pool-side `initialized` plus final reserve/share facts as the only terminal truth | current tests: `swap/src/contract_tests.rs`; missing characterization test for duplicate `UserPoolCreated`; missing characterization test for invalid pool application owner | contract state truth for processed marker; pool state truth for later funding | current_gap, target_gap, test_gap |
| G2-AL-001 | Contract handler layer | AddLiquidity Funding | pool/src/contract_inner/handlers/operation/add_liquidity.rs | primary: `AddLiquidityHandler::handle`; direct protocol entry: `PoolOperation::AddLiquidity` in `abi/src/swap/pool/mod.rs` | Requires both input amounts positive, sets origin from authenticated account, creates two linked fund requests, marks token0 request in-flight, marks second request created, and sends first meme funding request. | runtime `authenticated_account()`, `token_0()`, `token_1()`; state transfer id counter through `create_fund_request(...)` | state `fund_requests[transfer_id_0]`, `fund_requests[transfer_id_1]`; state updated next link; outgoing `PoolMessage::RequestFund` | pending funding state; outgoing message | authenticated account is origin; missing source-chain workflow binding | amount positivity exists; transfer id linkage exists; no intent id separate from transfer ids | fund request status is partial truth; terminal truth for full add-liquidity workflow is missing | target requires two-leg intent with explicit pending, funded, finalized, and failed states before reserve update and LP mint | current tests: `pool/src/contract_tests.rs`, `pool/tests/meme_native_pool.rs`; missing characterization test for second native request status after native funding; missing characterization test for duplicate fund success | contract state truth | current_gap, target_gap, test_gap |
| G2-AL-001A | Contract handler layer | AddLiquidity Funding | swap/src/contract_inner/handlers/message/user_pool_created.rs | primary: `UserPoolCreatedHandler::handle`; direct call payload: `PoolOperation::AddLiquidity` in `abi/src/swap/pool/mod.rs` | First user liquidity for a newly created user pool is triggered from `SwapMessage::UserPoolCreated`: the handler marks the pool application processed, converts pool application account owner into an application id, and calls pool `AddLiquidity` with both min amounts and block timestamp set to `None`. | state `mark_user_pool_created(pool_application)`; message amounts and recipient | processed marker; runtime `call_application(pool_application, PoolOperation::AddLiquidity)` | initial add-liquidity kickoff; cross-application call | missing message-origin check; missing authenticated-caller check | amount source is `UserPoolCreated`; LP recipient source is message `to`; no slippage protection because min fields are `None` | processed marker is duplicate guard; add-liquidity terminal truth is missing | target requires user-pool-created trigger to bind to pool creation intent and pool-local add-liquidity intent | current tests: `swap/src/contract_tests.rs`; missing characterization test for first add-liquidity trigger payload; missing characterization test for duplicate processed marker blocking repeated call | contract state truth for processed marker | current_gap, target_gap, test_gap |
| G2-AL-001B | Contract handler layer | AddLiquidity Funding | pool/src/contract_inner/handlers/request_meme_fund.rs | primary: `RequestMemeFundHandler::handle`; direct protocol entry: `PoolMessage::RequestFund` in `abi/src/swap/pool/mod.rs` | Resolves meme token creator chain id and sends `PoolMessage::RequestFund { token, transfer_id, amount }` to that chain. | runtime `token_creator_chain_id(token)` | outgoing `PoolMessage::RequestFund` | meme funding request | missing workflow source validation; send is not tracked in current runtime | transfer id and amount are copied from caller; token creator chain is resolved by runtime meme call | missing | target requires funding request to be tracked and bound to expected token leg and workflow state | current tests: `pool/src/contract_tests.rs`; missing characterization test for request destination chain; missing characterization test for token creator chain lookup failure | none | current_gap, target_gap, test_gap |
| G2-AL-001C | Contract handler layer | AddLiquidity Funding | pool/src/contract_inner/handlers/message/request_fund.rs | primary: `RequestFundHandler::handle`; direct call payload: `MemeOperation::TransferToCaller` in `abi/src/meme.rs` | On token creator chain, calls meme application `TransferToCaller` for requested amount, then sends `FundSuccess` to message origin chain when response is `MemeResponse::Ok` and `FundFail` when response is `MemeResponse::Fail`. | runtime `require_message_origin_chain_id()`, `call_application(token, TransferToCaller)` | outgoing `PoolMessage::FundSuccess`; outgoing `PoolMessage::FundFail` | meme funding response; success receipt; fail receipt | requires message origin chain id; missing authenticated-caller check; missing pool application source validation | token, transfer id, and amount come from message; no state lookup on token creator chain | success and fail receipts are outgoing facts; pool-side terminal truth depends on later receipt handling | target requires response to carry enough source, token, owner, leg, amount, and workflow identity for validation | current tests: `pool/src/contract_tests.rs`; missing characterization test for `MemeResponse::Fail` mapping to `FundFail`; missing characterization test for invalid meme response panic | none | current_gap, target_gap, test_gap |
| G2-AL-002 | Contract handler layer | AddLiquidity Funding | pool/src/contract_inner/handlers/message/fund_success.rs | primary: `FundSuccessHandler::handle`; callee: `FundSuccessHandler::add_liquidity_fund_success` in `pool/src/contract_inner/handlers/message/fund_success.rs` | Loads fund request, marks it success, advances the paired request, funds native pool application creation account when needed, transfers meme assets to application creation account, and sends `PoolMessage::AddLiquidity` after both requests are available. | state `fund_request(transfer_id)`; runtime `token_0()`, `application_creator_chain_id()`, `application_creation_account()` | state `update_fund_request(...)`; runtime native funding; meme transfer calls; outgoing `PoolMessage::AddLiquidity` | funding success state; native transfer; meme transfer; outgoing settlement message | missing message-origin check; missing authenticated-caller check; missing token application validation on success receipt | transfer id lookup exists; status overwritten to success; native second request can remain `Created` while native amount is funded | `FundStatus::Success` is per-leg truth; full workflow terminal truth is missing until `AddLiquidity` executes | target requires explicit both-leg funded state and failed opposite-leg claim credit | current tests: `pool/src/contract_tests.rs`; missing characterization test for native second request status; missing characterization test for success receipt for unknown transfer id | contract state truth | current_gap, target_gap, test_gap |
| G2-AL-002A | Contract handler layer | AddLiquidity Funding | pool/src/contract_inner/handlers/message/fund_fail.rs | primary: `FundFailHandler::handle`; direct protocol entry: `PoolMessage::FundFail` in `abi/src/swap/pool/mod.rs` | Loads fund request by transfer id, sets status to `FundStatus::Fail`, records error string, and writes the request back. | state `fund_request(transfer_id)` | state `update_fund_request(...)` | failed funding state | missing message-origin check; missing authenticated-caller check | transfer id lookup exists; no token, leg, amount, or owner validation against original request source | `FundStatus::Fail` is per-request fail truth; linked opposite-leg workflow terminal truth is missing | target requires failed leg to close the workflow state and credit already-custodied opposite-leg value to claim balance | current tests: `pool/src/contract_tests.rs`; missing characterization test for add-liquidity first-leg fail; missing characterization test for second-leg fail after first leg success | contract state truth | current_gap, target_gap, test_gap |
| G2-AL-003 | Contract handler layer | AddLiquidity Funding | pool/src/contract_inner/handlers/message/add_liquidity.rs | primary: `AddLiquidityHandler::handle`; direct protocol entry: `PoolMessage::AddLiquidity` in `abi/src/swap/pool/mod.rs` | Calculates accepted pair amounts, mints LP to explicit recipient or origin, refunds excess through meme transfer and native transfer, builds transaction, and sends `NewTransaction` to current pool chain. | state `try_calculate_swap_amount_pair(...)`; runtime `system_time()`, `token_0()`, `token_1()`, `application_id()`, `chain_id()` | state `add_liquidity(...)`; meme transfer refunds; native refund transfer; transaction built through state; outgoing `PoolMessage::NewTransaction` | LP mint; reserve mutation; refund; transaction fact | missing message-origin check; missing authenticated-caller check | min amount checks happen inside calculation; LP recipient defaults to origin; accepted amount uses limiting side | reserve and LP state are success truth; refund delivery has no terminal truth | target requires excess and post-custody failure value to credit claim balances, not rely on direct payout | current tests: `pool/tests/meme_native_pool.rs`, `pool/tests/meme_meme_pair.rs`; missing characterization test for excess native refund failure; missing characterization test for stale `AddLiquidity` message | contract state truth; transaction projection truth after `NewTransaction` | current_gap, target_gap, test_gap |
| G2-AL-004 | Persistent state layer | AddLiquidity Funding | pool/src/state/state_impl.rs | primary: `PoolState::create_fund_request`; callee: `PoolState::add_liquidity` in `pool/src/state/state_impl.rs`; callee: `PoolState::build_transaction` in `pool/src/state/state_impl.rs` | Allocates transfer ids from a counter, stores fund requests, mints shares, updates reserves and `k_last`, and builds transaction facts. | `transfer_id`, `fund_requests`, pool reserves, total supply, shares | `fund_requests`, `shares`, `total_supply`, pool reserves, `k_last`, transaction id | LP mint; reserve mutation; transaction fact | state methods do not validate source; handler layer owns checks | amount positivity enforced in mint shares; no workflow status gate blocks stale settlement message | pool reserves and shares are terminal economic truth; no add-liquidity intent terminal truth exists | target requires state machine fields for two-leg funding and finalization | current tests: `pool/src/contract_tests.rs`; missing characterization test for stale add-liquidity message executing after fund fail; missing characterization test for fund request overwrite path | contract state truth | current_gap, target_gap, test_gap |
| G2-SW-001 | Contract handler layer | Swap Funding And Settlement | pool/src/contract_inner/handlers/operation/swap.rs | primary: `SwapHandler::handle`; direct protocol entry: `PoolOperation::Swap` in `abi/src/swap/pool/mod.rs` | Requires exactly one input side, creates one fund request for meme input, sends meme funding request, and for native input funds pool application creation chain then sends `PoolMessage::Swap`. | runtime `authenticated_account()`, `token_0()`, `token_1()`, `application_creator_chain_id()`; state `create_fund_request(...)` | state fund request for meme input; runtime native funding; outgoing `PoolMessage::RequestFund` or `PoolMessage::Swap` | pending funding state; native transfer; outgoing settlement message | authenticated account is origin; missing workflow identity; missing bounce handling | one-sided positive input check exists; min-output values are copied into fund request or message | fund request status is partial truth; native direct branch has no fund request truth | target requires swap intent state before economics finalize and claim credit for output and refund | current tests: `pool/tests/meme_native_pool.rs`; missing characterization test for native input branch lacking fund request; missing characterization test for bounced `PoolMessage::Swap` | contract state truth for meme input request; none for native input request | current_gap, target_gap, test_gap |
| G2-SW-001A | Contract handler layer | Swap Funding And Settlement | pool/src/contract_inner/handlers/message/fund_success.rs | primary: `FundSuccessHandler::swap_fund_success`; direct protocol entry: `PoolMessage::FundSuccess` in `abi/src/swap/pool/mod.rs`; direct settlement message: `PoolMessage::Swap` in `abi/src/swap/pool/mod.rs` | For swap fund success, transfers meme input to the pool application creation account, builds `PoolMessage::Swap` with input side selected by whether the funded token equals token0, and sends settlement to the pool application creator chain. | state `fund_request(transfer_id)`; runtime `token_0()`, `application_creation_account()`, `application_creator_chain_id()` | state `update_fund_request(...)`; meme transfer to application creation account; outgoing `PoolMessage::Swap` | meme input custody movement; funding success state; outgoing settlement message | missing message-origin check; missing authenticated-caller check | transfer id selects fund request; input side derived from `fund_request.token == Some(token_0)`; min-output copied from request | per-request success truth exists; swap settlement terminal truth is missing until `PoolMessage::Swap` executes | target keeps swap as message-driven custody plus settlement with no persisted swap intent; successful custody advances through direct settlement message and failure after custody credits input back to claim balance | current tests: `pool/src/contract_tests.rs`; missing characterization test for token0 swap success payload; missing characterization test for token1 meme swap success payload | contract state truth | current_gap, target_gap, test_gap |
| G2-SW-002 | Contract handler layer | Swap Funding And Settlement | pool/src/contract_inner/handlers/message/fund_fail.rs | primary: `FundFailHandler::handle`; direct protocol entry: `PoolMessage::FundFail` in `abi/src/swap/pool/mod.rs` | Loads fund request by transfer id, marks status fail, records error string, and writes the fund request back. | state `fund_request(transfer_id)` | state `update_fund_request(...)` with `FundStatus::Fail` and error | failed funding state | missing message-origin check; missing authenticated-caller check | transfer id lookup exists; token, amount, and requester are not verified against message source | `FundStatus::Fail` is current per-request fail truth | target requires failed swap input funding to terminate the message-driven swap path without reserve mutation and to credit claim balance only for value that has already entered pool custody | current tests: `pool/src/contract_tests.rs`; missing characterization test for swap fund fail with unknown transfer id; missing characterization test proving failed swap fund request does not mutate reserves | contract state truth | current_gap, target_gap, test_gap |
| G2-SW-003 | Contract handler layer | Swap Funding And Settlement | pool/src/contract_inner/handlers/message/swap.rs | primary: `SwapHandler::handle`; callee: `SwapHandler::do_swap` in `pool/src/contract_inner/handlers/message/swap.rs` | Calculates output, validates min output, zero output, invariant, and native output balance, refunds input before returning errors, catches `do_swap` errors, logs them, and returns `Ok(None)`. On success it updates reserves, builds transaction, sends `NewTransaction`, and dispatches output. | state `calculate_swap_amount_0(...)`, `calculate_swap_amount_1(...)`, `pool().validate_swap_invariant(...)`, `reserve_0()`, `reserve_1()`; runtime `owner_balance(...)`, `token_0()`, `token_1()`, `application_id()`, `system_time()`, `chain_id()` | refund transfers; state `liquid(...)`; transaction built through state; outgoing `PoolMessage::NewTransaction`; meme output transfer; native output transfer | refund; reserve mutation; payout; transaction fact | missing message-origin check; missing authenticated-caller check | min-output and invariant checks exist; output recipient defaults to origin; refund happens before returned error is swallowed | successful reserve mutation is terminal truth; refund-plus-`Ok(None)` has no explicit failed workflow truth | target requires input refund and successful output to converge through claim balances | current tests: `pool/tests/meme_native_pool.rs`; missing characterization test proving failed settlement returns `Ok(None)` after refund; missing characterization test for output transfer bounce after reserve mutation | contract state truth; transaction projection truth after `NewTransaction` | current_gap, target_gap, test_gap |
| G2-SW-004 | Contract handler layer | Swap Funding And Settlement | pool/src/contract_inner/handlers/refund.rs | primary: `RefundHandler::handle`; direct callee: `TransferMemeFromApplicationHandler::handle` in `pool/src/contract_inner/handlers/transfer_meme_from_application.rs` | Refunds token1 first through meme transfer when token1 is meme, native transfer when token1 is native, then refunds token0 through meme transfer. | runtime `token_1()`, `token_0()`, `application_id()` | meme transfer calls; runtime native transfer | refund payout | missing workflow terminal check; missing bounce handling for meme refund delivery | amount defaults absent side to zero; only positive amounts are transferred | missing for refund delivery success and failure | target credits refund amount to claim balance instead of relying on immediate direct payout | current tests: `pool/src/contract_tests.rs`; missing characterization test for refund meme transfer failure; missing characterization test for refund native transfer amount source | none | current_gap, target_gap, test_gap |
| G2-RL-001 | Contract handler layer | RemoveLiquidity Funds Exit | pool/src/contract_inner/handlers/operation/remove_liquidity.rs | primary: `RemoveLiquidityHandler::handle`; direct protocol entry: `PoolOperation::RemoveLiquidity` in `abi/src/swap/pool/mod.rs` | Requires positive LP amount, reads authenticated account as origin, and sends `PoolMessage::RemoveLiquidity` to the pool application creator chain. | runtime `authenticated_account()`, `application_creator_chain_id()` | outgoing `PoolMessage::RemoveLiquidity` | outgoing message dispatch | authenticated account is LP burn origin; missing workflow identity | positive liquidity check exists; min outputs and recipient are copied from operation | missing | target requires remove intent state and claim-balance accrual after burn/decrease | current tests: `pool/tests/meme_native_pool.rs`; missing characterization test for absent authenticated signer using chain owner account; missing characterization test for bounced remove message | none | current_gap, target_gap, test_gap |
| G2-RL-002 | Contract handler layer | RemoveLiquidity Funds Exit | pool/src/contract_inner/handlers/message/remove_liquidity.rs | primary: `RemoveLiquidityHandler::handle`; direct protocol entry: `PoolMessage::RemoveLiquidity` in `abi/src/swap/pool/mod.rs` | Removes liquidity from origin, pays token0 and token1 directly to explicit recipient or origin, builds transaction, and sends `NewTransaction`. | runtime `system_time()`, `token_0()`, `token_1()`, `application_id()`, `chain_id()`; state `remove_liquidity(...)` | state LP burn and reserve mutation; meme payout transfers; native payout transfer; transaction built through state; outgoing `PoolMessage::NewTransaction` | LP burn; protocol fee mint; reserve mutation; payout; transaction fact | missing message-origin check; missing authenticated-caller check | payout recipient defaults to origin; min-output validation is delegated to state; token identity comes from runtime parameters | reserve, share, and transaction state are success truth; payout delivery terminal truth is missing | target requires owed token amounts to become claim balances; direct payout is not the funds-closure boundary | current tests: `pool/tests/meme_native_pool.rs`, `pool/tests/meme_meme_pair.rs`; missing characterization test for payout transfer bounce after LP burn; missing characterization test for stale remove message | contract state truth; transaction projection truth after `NewTransaction` | current_gap, target_gap, test_gap |
| G2-RL-003 | Persistent state layer | RemoveLiquidity Funds Exit | pool/src/state/state_impl.rs | primary: `PoolState::remove_liquidity`; callee: `PoolState::burn` in `pool/src/state/state_impl.rs`; callee: `PoolState::mint` in `pool/src/state/state_impl.rs` | Mints protocol fee share, calculates owed amounts, burns LP from origin, reduces reserves, updates `k_last`, and returns owed token amounts. | pool reserves; total supply; shares; fee recipient | shares; total supply; pool reserves; `k_last` | protocol fee mint; LP burn; reserve mutation | state method trusts handler origin; no source validation | liquidity must be less than or equal to share; min outputs checked by calculation | pool state is terminal economic truth; payout terminal truth is separate and missing | target requires single transition that records owed claim balances with burn/decrease | current tests: `pool/src/contract_tests.rs`; missing characterization test for protocol fee mint in remove path; missing characterization test for min-output failure preserving LP | contract state truth | current_gap, target_gap, test_gap |
| G2-CL-001 | Protocol definition layer | Unified Claim Target Gap | abi/src/swap/pool/mod.rs | primary: `PoolOperation`; direct message enum: `PoolMessage` in `abi/src/swap/pool/mod.rs` | Pool ABI exposes add liquidity, remove liquidity, swap, and administrative operations. It does not define funding `Claim`, claim-balance credit, claiming-balance movement, claim success acknowledgement, claim fail acknowledgement, or claim bounce carrier. | none | none | none | missing funding claim ABI source and auth checks | missing token identity, owner, amount, and in-flight claiming identity for claim | missing | target requires `PoolOperation::Claim { token_identity, amount }` plus message support for meme claim success, fail, and bounce convergence | current tests: `pool/src/contract_tests.rs`; missing characterization test proving no funding claim ABI exists; missing target tests for native claim balance decrement and meme claiming balance movement | none | current_gap, target_gap, test_gap |
| G2-CL-002 | Persistent state layer | Unified Claim Target Gap | pool/src/interfaces/state.rs | primary: `StateInterface`; direct implementation target: `PoolState` in `pool/src/state/state_impl.rs` | State interface exposes pool, reserves, fund requests, LP, liquidity, transaction, and fee setters. It does not expose `claim_balances` or `claiming_balances`. | none | none | none | missing claim owner authorization state boundary | missing token-first owner-second claim balance API | missing | target requires aggregated two-level claim and claiming balance maps keyed by pool token identity then owner account | current tests: `pool/src/contract_tests.rs`; missing characterization test proving current direct payout sites do not persist claim balances; missing target tests for aggregated balance merge | none | current_gap, target_gap, test_gap |
| G2-CL-003 | Contract handler layer | Unified Claim Target Gap | pool/src/contract_inner/handlers/message/add_liquidity.rs | primary: `AddLiquidityHandler::handle`; direct current payout site: excess refund in `pool/src/contract_inner/handlers/message/add_liquidity.rs` | Excess token0 and token1 are returned through direct meme transfer and native transfer after LP mint and reserve update. | runtime `token_0()`, `token_1()`, `application_id()`; state accepted amount calculation | direct transfer calls; LP mint; reserve mutation | refund payout | missing claim owner authorization because no claim path exists | owner is origin; token identity is runtime token; amount is `input - accepted` | missing for refund delivery | target credits excess to `claim_balances[token][origin]` as terminal accounting after finalization | current tests: `pool/tests/meme_native_pool.rs`; missing characterization test for excess refund amount and owner; missing target test for excess claim balance credit | contract state truth; no claim product truth | current_gap, target_gap, test_gap |
| G2-CL-004 | Contract handler layer | Unified Claim Target Gap | pool/src/contract_inner/handlers/message/swap.rs | primary: `SwapHandler::do_swap`; direct current payout site: swap output dispatch in `pool/src/contract_inner/handlers/message/swap.rs` | Successful swap updates reserves and dispatches output through meme transfer or native transfer. Failed settlement calls refund handler and then message handler returns `Ok(None)`. | state reserves and output calculation; runtime token ids and application account | state reserve mutation; direct output transfer; direct refund transfer | payout; refund; reserve mutation | missing claim execution auth because output is direct payout | output recipient is message `to` when present and origin when absent; output amount is calculated by pool math | successful reserve mutation is terminal truth; output delivery terminal truth is missing | target credits swap output and post-custody refund to claim balances | current tests: `pool/tests/meme_native_pool.rs`; missing characterization test for output owner/token/amount mapping to future claim credit; missing target test for swap output claim balance | contract state truth; transaction projection truth; no claim product truth | current_gap, target_gap, test_gap |
| G2-CL-005 | Contract handler layer | Unified Claim Target Gap | pool/src/contract_inner/handlers/message/remove_liquidity.rs | primary: `RemoveLiquidityHandler::handle`; direct current payout site: remove-liquidity payout in `pool/src/contract_inner/handlers/message/remove_liquidity.rs` | After LP burn and reserve decrease, owed token amounts are paid directly through meme transfer and native transfer. | state `remove_liquidity(...)`; runtime token ids and application account | LP burn; reserve mutation; direct payout; transaction fact | payout; LP burn; reserve mutation | missing claim execution auth because payout is direct | owner is payout recipient `to` when present and origin when absent; token identities come from runtime token parameters; amounts come from state remove result | pool state is terminal truth; payout delivery truth is missing | target credits remove-liquidity owed amounts to claim balances after burn/decrease | current tests: `pool/tests/meme_native_pool.rs`; missing characterization test for remove payout owner/token/amount mapping; missing target test for remove claim balance credit | contract state truth; transaction projection truth; no claim product truth | current_gap, target_gap, test_gap |
| G2-CL-006 | Contract handler layer | Unified Claim Target Gap | meme/src/contract_inner/handlers/message/redeem.rs | primary: `RedeemHandler::handle`; direct protocol entry: `MemeMessage::Redeem` in `abi/src/meme.rs` | Meme redeem transfers meme-chain balance from current-chain owner slot to the owner account and does not touch pool claim state. | runtime `chain_id()`; state `balance_of(from)` when amount absent | state `transfer(from, owner, amount)` | meme transfer; meme-chain balance exit | missing message-origin validation | owner and amount come from `MemeMessage::Redeem`; absent amount means full source balance | meme balance mutation is terminal truth for redeem | meme redeem is separate from pool-held claim balances; current implementation creates no pool claim balance for redeem | current tests: `meme/src/contract_tests.rs`; missing characterization test confirming redeem does not create pool claim state | contract state truth | current_gap, target_gap, test_gap |
| G2-BR-001 | Runtime abstraction layer | Tracked Reject / Bounce Gap | runtime/src/interfaces/contract.rs | primary: `ContractRuntimeContext::send_message`; direct capability symbols: `ContractRuntimeContext::open_chain`, `ContractRuntimeContext::change_application_permissions`, `ContractRuntimeContext::application_permissions` in `runtime/src/interfaces/contract.rs` | Runtime interface exposes authenticated caller, message send, app call, direct transfer, combined transfer, app creation, chain opening, and app-permission capabilities. It does not expose bounce detection and does not expose close-chain execution capability. | none | none | runtime capability surface | missing bounce receive API; missing close-chain execution API | send API has no tracking policy parameter and no workflow id | missing | target requires runtime capability for tracked send by default, bounce detection, and close-chain execution after cleanup | current tests: no `runtime/src/**/*test*.rs` and no `runtime/tests/**/*.rs` in Gate 1B; missing runtime test for send authentication plus tracking; missing runtime test for close-chain capability exposure | none | current_gap, target_gap, test_gap |
| G2-BR-002 | Runtime abstraction layer | Tracked Reject / Bounce Gap | runtime/src/contract.rs | primary: `ContractRuntimeAdapter::send_message`; direct trait symbol: `ContractRuntimeContext::send_message` in `runtime/src/interfaces/contract.rs` | Sends messages through `prepare_message(message).with_authentication().send_to(destination)` and does not attach `.with_tracking()`. | runtime message send context | outgoing authenticated message | outgoing message dispatch | authentication is attached centrally; tracking is missing centrally; bounce context is not read | no workflow id, bounce policy, claim compensation metadata, or close-chain trigger | missing | target requires `.with_tracking()` in the central runtime adapter and receive-side bounce API | current tests: no runtime send tests exist; missing characterization test proving current send lacks tracking; missing target test proving tracked bounce is observable | none | current_gap, target_gap, test_gap |
| G2-BR-003 | Runtime abstraction layer | Tracked Reject / Bounce Gap | base/src/handler.rs | primary: `HandlerOutcome::with_message`; direct struct symbol: `HandlerMessage` in `base/src/handler.rs` | Handler outcome stores only destination chain id and message payload. | none | `HandlerOutcome.messages` | outgoing message carrier | no auth or tracking policy at handler outcome layer; runtime adapter supplies auth | no workflow id, transfer id, bounce policy, terminal state, or claim metadata in handler message | missing | target keeps tracking centralized in runtime while handlers must carry enough workflow identity in message payload and state | current tests: no `base/src/**/*test*.rs` and no `base/tests/**/*.rs` in Gate 1B; missing characterization test for handler outcome message shape | none | current_gap, target_gap, test_gap |
| G2-BR-004 | Contract dispatch boundary layer | Tracked Reject / Bounce Gap | pool/src/contract_impl.rs | primary: `PoolContract::on_message`; direct runtime symbol: `ContractRuntimeContext::send_message` in `runtime/src/interfaces/contract.rs` | Dispatches pool messages through handler factory, panics on handler error, drains outgoing handler messages, and sends them through runtime send. | handler outcome messages | outgoing runtime messages | outgoing message dispatch | dispatch does not branch on bounced messages; handler error panics | no workflow terminal state is written at dispatch | missing | target requires bounced-message dispatch entries for fund request, settlement, payout, refund, and claim messages | current tests: `pool/src/contract_tests.rs`; missing characterization test for bounced pool message; missing characterization test for dispatch panic versus failed workflow state | none | current_gap, target_gap, test_gap |
| G2-BR-005 | Product entry and truth boundary layer | Tracked Reject / Bounce Gap | service/kline/src/normalizer/decode_result_normalizer.py | primary: `DecodeResultNormalizer.normalize_item`; direct family resolver: `ApplicationEventFamilyResolver.resolve` in `service/kline/src/normalizer/application_event_family_resolver.py` | Normalization preserves rejected execution status and reject reason as observational event payload and normalization status. | raw decoded item `execution_status`, `rejected`, `reject_reason` | normalized event payload and status | rejected fact projection | not contract auth; observational source only | no workflow id or claim compensation state is derived | rejected normalized event is observational truth; contract terminal truth is missing | target projections must expose pending, rejected, bounced, failed, claimable, and claiming states from protocol facts | current tests: `service/kline/tests/decode_result_normalizer_test.py`, `service/kline/tests/normalized_event_materializer_test.py`; missing test connecting bounced funding message fact to funding workflow read model | service/kline projection truth only | current_gap, target_gap, test_gap |
| G2-PT-001 | Product entry and truth boundary layer | Projection And Product Truth | webui-v2/src/components/meme/CreateMemeView.vue | primary: `createMeme`; direct click entry: `onCreateMemeClick` in `webui-v2/src/components/meme/CreateMemeView.vue` | Validates local form fields, sets creator from `user.User.account()`, sets swap creator chain id from `creatorChainId.creatorChainId('swap')`, copies initial liquidity into parameters when enabled, and calls `Wallet.createMeme(...)`. | `argument`, `parameters`, `hasInitialLiquidity`, `initialLiquidity`, `user.User.account()`, `creatorChainId.creatorChainId('swap')`, blob and AMS existence checks | `parameters.creator`; `parameters.swapCreatorChainId`; `parameters.initialLiquidity`; operation submission through `Wallet.createMeme(...)` | product entry; operation submission | frontend validates fields but is not a security boundary; contract must validate signer, creator, amounts, and workflow identity | checks positive initial liquidity values when initial liquidity is enabled; checks positive mining supply when mining is enabled; no workflow identity | missing | target requires product entry to surface pending, failed, claimable, and claiming workflow truth from projections | current tests: `webui-v2/src/components/meme/createMemeMetadata.test.ts`; missing frontend baseline test for create-meme initial liquidity serialization; missing frontend baseline test for mining field serialization | frontend cache and wallet state; generated GraphQL dependency fact only when declarations request generated types | current_gap, target_gap, test_gap |
| G2-PT-002 | Product entry and truth boundary layer | Projection And Product Truth | webui-v2/src/components/pools/poolFlow.ts | primary: `resolveLiquiditySubmissionMode`; direct store helper: `findPoolByIdentity` in `webui-v2/src/stores/swap/poolIdentity.ts` | Normalizes pool pairs, detects pair existence, and returns `add-liquidity` when the normalized pair exists and `create-pool` when it does not. | pool list; selected token pair; constants `LINERA_NATIVE_ID` | frontend route and submission mode decision | product routing | frontend decision is not a security boundary | pair normalization orders native token second and sorts non-native token ids; active-pair existence comes from provided pool list | frontend route decision is not protocol terminal truth | target requires projection-backed pool lifecycle and pending shell visibility, not only live routing state | current tests: `webui-v2/src/components/pools/poolFlow.test.ts`, `webui-v2/src/stores/swap/poolIdentity.test.ts`; missing baseline test for pending pool shell not being treated as active pool | frontend cache; live GraphQL query dependencies | current_gap, target_gap, test_gap |
| G2-PT-003 | Product entry and truth boundary layer | Projection And Product Truth | webui-v2/wasm/src/lib.rs | primary: `graphql_deserialize_swap_operation`; direct serialization symbols: `graphql_deserialize_pool_operation`, `graphql_deserialize_proxy_operation` in `webui-v2/wasm/src/lib.rs`; direct schema modules: `webui-v2/wasm/src/fake_swap.rs`, `webui-v2/wasm/src/fake_pool.rs`, `webui-v2/wasm/src/fake_proxy.rs` | Parses GraphQL operation query and variables, executes the corresponding fake schema mutation root, and returns the serialized operation payload JSON used by frontend submission. | query string; variables string; fake GraphQL schema execution result | serialized operation payload JSON | operation submission | wallet/provider authentication is outside serialization; contract remains authority | amount, account, application, and token ids depend on caller-provided serialized fields | missing | target requires explicit serialization support for claim operation and workflow-safe product entries | current tests: `webui-v2/src/utils/protocol.test.ts`; missing WASM baseline test for create-pool amounts; missing WASM baseline test for future claim operation serialization | generated operation payload dependency fact | current_gap, target_gap, test_gap |
| G2-PT-004 | Product entry and truth boundary layer | Projection And Product Truth | webui-v2/src/graphql/swap.ts | primary: `POOLS`; direct raw declaration: `CREATE_POOL` in `webui-v2/src/graphql/swap_raw.ts` | `POOLS` declares the frontend swap service query for pool discovery fields including pool id, token ids, pool application, prices, reserves, and creation time. `CREATE_POOL` declares the raw create-pool mutation shape used by operation serialization. | swap service query result | frontend GraphQL read result | product read | frontend query is not protocol auth | pool identity and token ids are read facts, not terminal workflow proof | live query truth exists for routing; terminal workflow truth for pending, failed, stalled, claimable, and claiming states is missing | target requires projection-backed product truth instead of live-query reconstruction for funding accounting | current tests: `webui-v2/src/stores/swap/poolIdentity.test.ts`; missing baseline test identifying exact swap GraphQL declaration used for pool existence; missing baseline test for `CREATE_POOL` amount and recipient variables | live GraphQL query; generated GraphQL dependency fact | current_gap, target_gap, test_gap |
| G2-PT-005 | Product entry and truth boundary layer | Projection And Product Truth | service/kline/src/normalizer/application_event_family_resolver.py | primary: `ApplicationEventFamilyResolver._resolve_pool_family`; direct meme resolver: `ApplicationEventFamilyResolver._resolve_meme_family` in `service/kline/src/normalizer/application_event_family_resolver.py`; direct swap resolver: `ApplicationEventFamilyResolver._resolve_swap_family` in `service/kline/src/normalizer/application_event_family_resolver.py`; direct proxy resolver: `ApplicationEventFamilyResolver._resolve_proxy_family` in `service/kline/src/normalizer/application_event_family_resolver.py` | Maps decoded pool, swap, meme, and proxy operations/messages/events into normalized event families, including rejected variants for selected messages. | decoded item payload kind, payload type, app type, rejected status | normalized event family | projection fact classification | observational only; no contract auth | no claimable balance family; no claiming balance family; no bounced workflow family | normalized event family is observational truth | target requires event families for claimable balances, claiming balances, pending workflows, failed workflows, and bounced funding messages | current tests: `service/kline/tests/decode_result_normalizer_test.py`, `service/kline/tests/message_decoder_registration_test.py`; missing normalizer test for claimable balance event family; missing normalizer test for bounced funding message family | service/kline projection truth | current_gap, target_gap, test_gap |
| G2-PT-006 | Product entry and truth boundary layer | Projection And Product Truth | service/kline/src/query/handlers/positions.py | primary: `PositionsHandler.get_positions`; direct read model: `PositionsReadModel.get_positions` in `service/kline/src/query/read_models/positions.py`; direct serializer: `PositionsSerializer.serialize_positions` in `service/kline/src/query/serializers/positions.py` | Positions query reads projection-backed positions and serializes only the position bounded-context fields allowed by `PositionsSerializer.ALLOWED_FIELDS`. | projection-backed positions read model; owner filter; status filter | serialized positions response | product read | API query has no contract auth; owner-scoped query validation belongs to service layer | owner and status filtering are product read filters, not funding workflow identity | projection is product truth for positions; funding workflow truth is missing | target requires projection-backed claimable, claiming, pending, failed, and stalled funding read models | current tests: `service/kline/tests/positions_api_test.py`, `service/kline/tests/query_stack_read_model_test.py`; missing API test for absence then addition of claimable and claiming balances | service/kline projection truth | current_gap, target_gap, test_gap |
| G2-EX-001 | Product entry and truth boundary layer | Projection And Product Truth | excluded:webui/ | primary: `webui/` | Excluded obsolete frontend directory. | none | none | none | none | none | missing | current frontend analysis must use `webui-v2/` only | none | none | excluded |
| G2-EX-002 | Product entry and truth boundary layer | Projection And Product Truth | excluded:webui-v2/src/__generated__/graphql/ | primary: `webui-v2/src/__generated__/graphql/` | Excluded generated GraphQL output. | none | none | none | none | none | missing | audited GraphQL source declarations remain under `webui-v2/src/graphql/*.ts` | none | generated GraphQL dependency fact only | excluded |
